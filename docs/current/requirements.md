---
codd:
  node_id: "req:project-requirements"
  type: requirement
  status: approved
  confidence: 0.95
---

# Comic Explorer 現行要件

## 目的と正本

Comic Explorerは、Windows上のローカルドライブをExplorer風の画面で探索し、
画像フォルダと対応書庫を安全に閲覧するデスクトップアプリである。本書は実装完了後の
保守契約の正本であり、受入結果は[verification.md](verification.md)、現在状態は
[status.md](status.md)、技術境界は[architecture.md](architecture.md)を正とする。

統合前の詳細な受入条件と判断履歴はGit履歴から参照・復元できる。変更時は本書の安定IDを
維持し、未測定項目を実装済みまたはPASSへ推定しない。

## 範囲

対象は、サイドバーで選択したローカルドライブ内の階層移動、catalog、thumbnail、検索、viewer、読書位置・
利用者metadata、app-local設定/cache、Windows配布である。BMP、JPEG/JPG、GIF、TIFF/TIF、PNG、
ICO、SVG、静止WebP、PDF、ZIP/CBZ/EPUB/RAR/CBR/7z/CB7/LZHを実装済み範囲とする。WICは画像形式ではなく、
Windows標準codecが扱うraster画像のdecode基盤として利用する。AVIFには実装済みの安全な
分類・拒否境界があるが、製品decodeの受入は未完了である。

対象外は、クラウド同期、外部書誌取得、telemetry、外部データ送信、library原本への自動変更である。
明示的な利用者操作に限り、catalogおよびfolder treeのcontext menuからOS全体を対象とするfile manager操作を提供する。

## 恒久安全原則

- すべての閲覧経路はlibrary原本、書庫、mtime、sidecarを変更しない。原本変更はfile managerの
  明示操作と確認を経た対象だけに限定する（REQ-MVP-017、REQ-MVP-021）。
- DB、設定、cache、temp、recovery、logはlibrary root外のapp-local領域だけへ保存する。
- 漫画データ、path、読書情報、利用状況を外部送信せず、通常利用はofflineで完結する（REQ-MVP-018）。
- UIへ絶対path、任意SQL、任意archive entry accessを公開せず、backendでroot境界とpathを検証する。
- 未測定、未実行、外部環境待ちはPASSではない。

## MVP機能要件

| 安定ID | 現行契約 |
|---|---|
| REQ-MVP-001 | library rootの登録画面や手入力設定を要求せず、release executableの起動時にconsoleやterminal windowを表示しないまま、PC配下のWindows論理ドライブをサイドバーへ列挙する。ドライブ選択時はそのドライブを安全境界として閲覧を開始し、最後に使用したドライブをapp-local設定から復元できる。旧版でfolder単位のlibrary rootが保存されている場合は、そのドライブと階層へ自動移行する。debug buildは診断用consoleを維持してよい。 |
| REQ-MVP-002 | 固定metadataを要求せず任意のfolder階層を扱う。 |
| REQ-MVP-003 | folder treeはPC、論理ドライブ、folderの階層をExplorerと同様に表示し、ドライブおよびfolderの展開・選択と現在folderを同期する。現在folderが変わった場合はancestorを取得・展開して選択nodeを表示範囲へ自動scrollし、tree上部の「現在位置へ移動」は利用者が折りたたんだ後でも同じ展開・scrollを明示的に再実行する。tree nodeは14px基準の約11px文字、24px固定行高、16px幅の展開記号列、14px幅の種類icon列を使い、展開記号とiconの間にnodeの左paddingを置かないcompact表示とする。利用者が開いたbranchは、current folderの移動、別driveとの往復、検索pane表示、treeの一時非表示では自動的に閉じず、明示的な個別折りたたみまたはtree上部の「すべて閉じる」で閉じる。tree上部には現在folderのExplorer形式absolute pathを表示する。別ドライブの選択時は安全境界とcatalogをそのドライブへ切り替える。 |
| REQ-MVP-004 | address入力、icon toolbarの戻る・進む・上へ・address移動、file menuの履歴移動を選択中ドライブ内で行う。addressにはWindows Explorerと同じ通常の絶対pathを表示し、内部canonical pathの拡張長接頭辞`\\?\`（UNCの場合は`\\?\UNC\`）を表示しない。address入力はWindows Explorerの「パスのコピー」による引用符付き絶対pathを受理し、slash・大文字小文字を正規化したうえでdriveとpath segment境界を厳密に判定する。別ドライブの絶対path入力時は安全境界をそのドライブへ切り替えて移動する。 |
| REQ-MVP-005 | folder、漫画folder、対応archive、画像をcatalogに表示し、未対応fileの種別にはfile名の拡張子をそのまま表示する。thumbnailを表示できないfolderとarchiveには、それぞれを判別できる専用iconを表示する。ファイル名を表示する一覧形式では名前の左端に種類iconを常時表示し、画像、folder（漫画folderを含む）、archive、PDF、未対応fileを表紙thumbnailの有無によらず判別できるようにする。ファイル名を表示しないカードグリッドでは、項目のaccessible nameとhover説明に名前と種別を保持する。 |
| REQ-MVP-006 | 対応archive・PDFは自然順の先頭pageから表紙thumbnailを生成し、catalogに直接表示する対応画像はその画像自身のthumbnailを生成する。folderは直下1階層だけにある対応画像を自然順で並べた先頭からthumbnailを生成し、サブfolder内の画像や直下archiveは表紙候補として走査しない。直下に対応画像がないfolder項目には専用iconを表示する。書庫内容一覧では表示範囲の画像項目だけを既存の安全な書庫readerと画像decode上限から遅延thumbnail化し、書庫内folder・入れ子書庫の移動、stale結果破棄、失敗時の項目別placeholderを保証して、一覧表示だけで全entryを一括展開しない。thumbnailは選択したsourceを含むfingerprintとcache鮮度を管理する。 |
| REQ-MVP-007 | catalog toolbar buttonから並べ替え、昇降順、一覧形式のmenuを操作し、viewer toolbarから巻末動作を操作して、設定を保存する。一覧形式は詳細リスト、小サムネイル、表紙グリッド、カードグリッド、情報カードの順に提示し、小サムネイル、表紙グリッド、カードグリッド、情報カードそれぞれのthumbnail幅を設定画面から変更して保存・復元できる。 |
| REQ-MVP-008 | 画像folderおよび対応archive内のBMP、JPEG/JPG、GIF、TIFF/TIF、PNG、ICO、SVG、静止WebPを1冊として相対pathの自然順で読み、catalogの画像を直接開いた場合は同じfolderのpage群を選択画像から開始する。raster画像はWICまたは専用decoderで実ピクセルを検証し、SVGはscriptと外部resourceを実行・取得せずに表示とthumbnail生成を行う。 |
| REQ-MVP-009 | ZIP/CBZ/EPUB、単一volume・非暗号化RAR4/RAR5（RAR/CBR）、非暗号化7z（7z/CB7）、LHA/LZH（LZH）を隣接展開せず、対応圧縮entryを検証し、格納画像を自然順で読む。対応書庫内の対応書庫は形式を混在でき、内側3階層・内側書庫64個・内側書庫の展開データ累計512 MiBを上限として再帰的に読む。分割RAR、暗号化書庫、未対応圧縮方式、EPUBのHTML本文組版は対象外とする。書庫ごとのentry数・展開後entry size・展開後合計size上限、危険path拒否、原本非破壊を共通に保証する。 |
| REQ-MVP-010 | catalogのfolder（漫画画像を含むfolderを含む）はダブルクリックまたはkeyboardでviewerを開かず、そのfolderへ移動する。親folderから子folderへ移動したcatalogは先頭から表示し、子folderから親folderへ戻ったcatalogは親folderを離れる直前のscroll位置を復元する。archive、PDF、画像は同じ操作でviewerへ開き、card内に重複する読むbuttonを置かず、終了後にcatalogの文脈を復元する。 |
| REQ-MVP-011 | viewerはページ送りレイアウトだけを使用し、縦スクロール・横スクロールのレイアウトとその選択UIは提供しない。旧profile・app-local設定に残る縦/横スクロール値は安全にページ送りへ正規化する。単pageを縦横比維持で表示し、範囲内移動とfitを提供する。page layoutの横幅フィットでは、利用者が設定したpage周囲余白を除く画像表示領域の横幅を使い、見開き時は設定したpage間隔だけを残す。page周囲余白の既定値は0pxとし、横幅フィットしたpageが表示領域より低い場合は残る縦余白を上下へ均等に配分し、高い場合は上端から画像下端まで設定値以外の末尾余白なしでscrollできるようにする。page layoutで縦が表示領域へ収まらない場合は上端から表示し、次page操作では未表示部分を下へ送って全体を閲覧した後に次pageへ進む。 |
| REQ-MVP-012 | 見開きは最大2pageとし、横長pageと末尾1pageを単独表示する。 |
| REQ-MVP-013 | 右開き・左開きを配置と移動へ一貫適用し、設定を保存する。 |
| REQ-MVP-014 | keyboard、wheel、swipe、Escで閲覧でき、画像stageの単clickではpage移動せず、double clickで全画面表示と解除を切り替える。viewer toolbarの操作を説明付きicon buttonで提供する。現在pageはtoolbarに表示せず、画像表示領域の下部に現在位置と総page数を示すslider式のpage移動barを置き、先頭pageを右端、末尾pageを左端として右から左へ任意のpageへ移動できる。前page・次pageとslideshowのbuttonは同じbarの右下に置き、sliderおよび現在位置と隣接させる。対応archiveを通常openした場合は全画面で開始し、全画面中はviewer toolbarとpage移動barを隠して、toolbarは画面上端、page移動barは画面下端へpointerを移動したときだけ表示する。viewerはopen時に全pageを画像として読み込まず、現在表示に必要なpageと最大4page先までを先読みする。見開き遷移は次の表示対象を先読みし、途中で1pageだけを表示せず見開き単位で滑らかに切り替える。viewer generationの古い結果を捨てる。 |
| REQ-MVP-014A | viewer toolbarは一覧へ戻る、作品名、表示枚数、倍率mode、段階zoom、現在倍率、しおり保存、その他の操作、全画面だけを常時表示する。左側は一覧へ戻ると作品名、右側は「表示枚数」「倍率」「しおりと補助操作」「全画面」の順に視覚的なgroupとして並べ、異なる目的のbuttonを無秩序に連続させない。前後pageとslideshowは画像表示領域下部のpage移動bar右下へ集約する。特に「一覧へ戻る」はtoolbarの先頭側のprimary controlとして、通常表示・全画面・狭幅のいずれでも折返しやoverflowで隠さない。巻末、pixel寸法、補助zoom、見開き調整、読み方向、しおり管理、分離、copy、回転・反転、filterは「その他の操作」から用途別の名前付きpanelとして開き、通常表示・全画面・狭幅のいずれでも画像領域やtoolbarのoverflowで切り落とさず、開閉と各既存機能の説明付きaccessible nameを維持する。レイアウト選択は提供しない。toolbarは利用可能な横幅内へ収めて横scrollbarを表示せず、狭幅では作品名と段階zoomを順に縮退する。Viewerを開いている間のnative title barは`Comic Explorer — <作品名>`とし、作品切替で更新し、Viewer終了時は`Comic Explorer`へ戻す。native title更新が利用できない環境ではViewerの表示・終了を妨げない。 |
| REQ-MVP-022 | 統合設定は利用目的の順に「一覧」「ビューワ」「画面とテーマ」「操作と入力」「プロファイル」を表示し、各カテゴリ内でも日常的に変更する項目を先、閾値・性能・診断向けの調整値を後に置く。検索は再編後の全項目を名前・説明・現在値から横断して表示する。全体テーマを正本とし、一覧だけの重複配色設定は提供しない。ページ送りへ固定済みの旧`layoutMode`と、画面に効果を持たない旧`wheelScrollFactor`は設定、named profile、export/import、app-local設定の公開schemaから除去する。旧profileまたは既存設定にあるこれらのkeyは読込み時に破棄して、残る有効設定を安全に復元する。 |
| REQ-MVP-015 | page keyを基準に読書位置をapp-local SQLiteへ保存・復元し、破損DBから安全に回復する。 |
| REQ-MVP-016 | 巻末では現在のcatalog sort順に従って次の漫画の1page目へ進む。folder内の画像群を1冊として閲覧している場合は親folderのcatalog sort順を使い、次の項目が対応archive、PDF、画像を含むfolderのいずれでもその先頭pageを開く。巻頭で前pageへ戻る操作は同じsort順の前の漫画を末尾pageから開き、前の漫画がなければ現在表示を維持する。 |
| REQ-MVP-017 | 閲覧、thumbnail、読書位置保存の前後でlibrary原本を非破壊に保つ。REQ-MVP-021の明示的file manager操作だけを例外とし、暗黙のrename、move、copy、create、deleteを行わない。 |
| REQ-MVP-018 | 外部通信、telemetry、crash upload、書誌取得、cloud同期を行わない。 |
| REQ-MVP-019 | 項目単位のaccess、missing、corrupt、unsupported errorから別操作へ復帰できる。 |
| REQ-MVP-020 | standalone PDF（`.pdf`）をcatalogの独立したPDF種別から1冊として開き、Windows標準PDF APIで各pageを上限付き画像へ変換して既存viewerの単page・見開き・読書位置・thumbnail・favorite・巻末遷移へ接続する。root包含確認後のWindows canonical path（拡張長接頭辞`\\?\`を含む）からも同じPDFを列挙・renderできること。PDF本体は1 GiB、page数は10,000、renderは最大辺16,384 pxかつ120,000,000 pixelsを上限とし、library root外へ展開・変換保存しない。非対応の暗号化PDF、破損PDF、空PDF、root外symlinkは分類した局所errorとする。 |
| REQ-MVP-021 | catalog context menuからrename、任意folderへのmove/copy、folder作成、ごみ箱delete、確認付き完全delete、Explorer表示、Windowsのアプリ選択、OS clipboardのCF_HDROPによるcut/copy/pasteを行う。folder treeのfolder nodeではcontext menu、Shift+F10、Ctrl+X/C/Vからcut/copy/pasteを行い、Deleteまたはcontext menuからcatalogと同じ確認dialogを経てごみ箱へ移動する。drive nodeではrootへのpasteだけを許可する。catalogのfolderを右clickしてpasteした場合は現在folderではなく右clickしたfolder内を宛先とする。catalogの選択項目またはfolder treeのfolderは、catalogまたはfolder treeに表示された同一drive内のfolderへdrag and dropしてmoveできる。アプリがclipboardへ設定したcut/copyはWindows Explorerへ貼り付けでき、Explorerが設定したfile clipboardもアプリ内folderへ貼り付けできる。相対sourceはcanonical library root内に限定し、folder pickerとOS clipboardで利用者が明示した外部pathだけを入出力に許可する。同名衝突、root外symlink、source自身または子孫へのmove/copy、不正名、欠落、access拒否、途中失敗を分類して通知し、成功後はcatalogとfolder treeを再列挙する。 |

## MVP非機能要件

| 安定ID | 現行契約 | 未完了境界 |
|---|---|---|
| NFR-MVP-001 | 1TB、10,000 files、1,000作品、1冊300pageを想定し、遅延処理、virtualize、10GiB thumbnail LRUを使う。folder移動では一覧の列挙と表示をthumbnail生成・cache保守の完了待ちから分離し、placeholderを先に表示してthumbnailを非同期に更新する。新しいfolderへ移動した場合は古いthumbnail要求をcancelし、cancel不能な生成中処理やpin解除が完了するまでfolder一覧を開く操作を待たせない。 | 10,000項目のWindows製品UI性能はBLOCKED。 |
| NFR-MVP-002 | cold起動3秒、cached一覧1秒、prefetch済みpage 100ms、10,000項目検索1秒、idle 250MiBを基準PCで測る。 | 現行release候補の基準PC測定はBLOCKED。 |
| NFR-MVP-003 | 14px基準のcompactな文字でtree、catalog、viewerをkeyboard操作でき、focusを視認できる。icon buttonは間隔、accessible name、hover説明を持ち、catalog cardは文字と操作欄を重ねず、tree labelは選択状態によらず背景と判別できる文字色で表示する。viewerの画像表示領域は既定で濃いグレーの市松模様を使い、設定から市松模様、濃灰、黒、明色の背景を選択して画像領域を判別可能にする。catalogの全5一覧形式は利用可能なペイン幅に応じて列数または表示する詳細列を縮退し、横方向にはみ出し・重なりを起こさない。小サムネイルと表紙グリッドでは画像の縦横比にかかわらずファイル名用の領域を画像とは別に確保し、文字を画像へ重ねず、種別ラベルを表示しない。カードグリッドは表紙グリッドより大きい既定thumbnailだけを縦横比2:3で表示し、ファイル名・種類icon・metadata用の視覚領域を設けない。情報カードでは横長cardの左に表紙、右にファイル名、種別、サイズ、更新日時を独立した情報領域として配置し、欠落metadataは安全な代替表示にする。小サムネイルでは画像をサムネイル枠の下端でclipし、ファイル名領域へ描画しない。サムネイル系4形式のthumbnail寸法は保存された形式別設定だけから決定し、ウィンドウやcatalogペインの幅変更では拡縮しない。ファイル名は本文より小さいcompactサイズで表示し、左端の種類iconを文字の省略や折返しから独立させる。お気に入りtoggleは小サムネイル、表紙グリッド、カードグリッドではサムネイルの左上、情報カードでは情報領域の右上、詳細リストでは左端の専用列に表示する小型buttonとする。設定画面を含む全dialogは共通のheader、余白、control、action、scroll表現を使い、狭い画面でもlabelと操作を重ねない。 | UIA、screen reader、high contrast、DPIはBLOCKED。 |
| NFR-MVP-004 | lockfile全依存を再配布可能licenseに限定し、SBOMとTHIRD-PARTY-NOTICESを同期する。 | 既知の禁止・unknown licenseは0。 |
| NFR-MVP-005 | Windows 10 22H2 x64と対応中Windows 11 x64向けinstallerを生成する。installerにはWebView2 Runtimeまたはそのinstaller/bootstrapperを同梱・取得・実行する処理を含めず、installerとportable版はいずれも端末に導入済みのWebView2 Runtimeを利用する。同じ検証済みrelease executable、THIRD-PARTY-NOTICES、SBOMを1つのportable配布folderへまとめ、GitHub Actionsからinstallerとは別のZIP artifactとしてダウンロード可能にする。portable版はinstall処理や任意の追加fileを実行せず、展開後のexecutableを直接起動する。 | WebView2導入済みclean VMでのinstall/uninstallとportable ZIP起動、WebView2未導入時の失敗表示はBLOCKED。 |
| NFR-MVP-006 | 採用構成を再現可能なfixture、性能値、原本snapshot、配布検証で評価し、実測・推定・未測定を分ける。 | 外部環境の未測定を保持する。 |
| NFR-MAINT-001 | Windows filesystem上のCoDD、test、typecheck、buildは`.venv-windows`とWindows toolchainを使い、child exit codeと最終結果を保持する。 | Linux runnerで代替しない。 |

## 採用済みFeature要件

各行の安定IDは統合前要件から継承する。本表は保守時の現行契約だけを示し、過去の詳細は
Git履歴から参照する。

| Feature | 要件ID | 状態追跡ID | 現行契約 |
|---|---|---|---|
| FR-B01 表示倍率 | REQ-FR-B01-001, REQ-FR-B01-002, REQ-FR-B01-003, REQ-FR-B01-004, REQ-FR-B01-005, REQ-LEY-P1-003, REQ-LEY-P1-004, REQ-LEY-P1-006 | FUT-C-018, FUT-C-033, FUT-C-034, FUT-C-035, FUT-C-036, FUT-C-037, LEY-VIEWER-020, LEY-VIEWER-021, LEY-VIEWER-022 | 共通scale model、1%〜800%、fit幅/高さ/全体、原寸、状態維持、正方形のpointerルーペ、再起動復元。任意倍率の入力欄は1〜800%の整数で表示・入力し、内部値と保存形式には換算した倍率を使う。`+`/`-` は現在表示中の先頭pageの実表示倍率を基準に10%ずつ増減し、連続する逆方向操作で直前の表示倍率へ戻す。画像が表示領域を超えてもscrollbarを表示せず、pointer dragで任意の表示位置へpanできる。 |
| FR-B02 巻末動作 | REQ-FR-B02-001, REQ-FR-B02-002, REQ-FR-B02-003 | FUT-C-020, FUT-C-038, FUT-C-039, FUT-C-040, FUT-C-041 | viewer toolbarから`auto_next`、`confirm_next`、`return_library`、`stop`、`loop`を安全に適用し、sortと設定を維持する。folder内画像の閲覧では親catalogを巻順として使い、対応archive、PDF、画像folderを次巻・前巻候補とする。次巻・loop先は先頭page、巻頭からの前巻移動は末尾pageを開く。 |
| FR-B03 一覧形式 | REQ-FR-B03-001, REQ-FR-B03-002 | FUT-C-012, FUT-C-013, FUT-C-014 | `detail_list`、`small_thumbnail`、`cover_list`（表示名: 表紙グリッド）、`card_grid`（表示名: カードグリッド）、`reference_tile`（表示名: 情報カード）の順序、操作、focus、永続化を共通modelで扱う。toolbarの並べ替え条件と一覧表示形式のmenu triggerは省スペース化のため現在値だけを視覚表示し、機能名はaccessible nameとhover説明に保持する。表紙グリッドは縦長の表紙と直下のファイル名を中心にした画像優先layout、カードグリッドは表紙グリッドより大きい既定thumbnailだけを表示してファイル名・種類icon・metadataを視覚表示しない表紙専用layout、情報カードは横長cardの左側に縦長の表紙、右側に左揃えのファイル名、種別、サイズ、更新日時を置く情報優先layoutとし、外枠や色だけに依存せずシルエットと情報量で判別可能にする。カードグリッドでも項目名と種別はaccessible name、title、selection、context menuへ保持する。小サムネイルと表紙グリッドは種別ラベルを省き、画像とファイル名を別のlayout領域に置く。小サムネイルの画像はサムネイル枠の下端でclipしてファイル名領域への描画を防ぐ。カードグリッドは外側の枠線と内側余白を設けずthumbnailをcard全面へ配置し、行間・列間を他のthumbnail形式より狭い4pxにする。選択状態は外枠ではなくthumbnail上の色overlayで示す。カードグリッドのお気に入りtoggleは表紙の左上、情報カードでは情報領域の右上へ置き、card全体の既存open・selection・context menu操作と重ねない。仮想化した各rowは形式別thumbnail幅設定から導出した固定寸法、高さ、縦間隔を適用し、ウィンドウ幅変更時はthumbnailを拡縮せず列数だけを変更する。`small_thumbnail`、`cover_list`、`card_grid`、`reference_tile`の形式別thumbnail幅は安全な範囲へ検証し、profile、app-local設定、再起動復元へ含める。profile v3で導入した4つの必須幅をprofile v4/v5でも保持し、旧profile v1/v2には新しいカードグリッドの既定幅を補って移行する。詳細リストと情報カードでは種別を表示する。 |
| FR-B05 名前検索 | REQ-FR-B05-001, REQ-FR-B05-002, REQ-FR-B05-003, REQ-FR-B05-004, REQ-FR-B05-005 | FUT-C-010 | toolbarの検索buttonで開くside paneから正規化した名前検索、mixed result、結果移動、empty/clear/error、明示rescan、local-onlyを保証する。toolbarの検索buttonはcompactな外形を維持しつつ、検索と判別できる十分な大きさの虫眼鏡iconを表示する。検索範囲は「現在のフォルダー」「現在のライブラリ全体」「指定した複数の場所」から明示選択し、サブフォルダー走査と結果へ含めるfolder/file種別を別の肯定形controlとして表示する。sizeの以上/以下、更新日時の以降/以前/期間、結果を開いた後も検索結果を表示する設定は詳細条件へまとめ、最低1つの結果種別を必須とする。名前による絞り込みはこの検索side paneへ一本化し、検索結果表示中の件数・選択表示は検索結果を正本とする。 |
| FR-B06 お気に入り | REQ-FR-B06-001, REQ-FR-B06-002, REQ-FR-B06-003, REQ-FR-B06-004, REQ-FR-B06-005 | FUT-C-011, FUT-C-021 | stable identity、冪等add/remove、quick access、missing/moved再解決、migrationと再起動保存を保証する。 |
| FR-B07 読書情報 | REQ-FR-B07-001, REQ-FR-B07-002, REQ-FR-B07-003, REQ-FR-B07-004, REQ-FR-B07-005 | FUT-C-023, FUT-R-004, FUT-R-005 | item identityごとのmemo、成功open history、rating、schema migration、原本非破壊を保証する。 |
| FR-B08 静止WebP | REQ-FR-B08-001, REQ-FR-B08-002, REQ-FR-B08-003, REQ-FR-B08-004, REQ-FR-B08-005 | FUT-C-005 | folder/ZIP/CBZ/EPUB/RAR/CBR/7z/CB7/LZHの静止WebPを列挙・表示・thumbnail化し、corrupt/animatedを局所errorにしてlicense gateを通す。 |
| FR-B09 ライブラリ診断 | REQ-FR-B09-001, REQ-FR-B09-002, REQ-FR-B09-003 | — | ライブラリをread-onlyで確認し、前回診断との追加・変更・欠落、重複、破損した対応書庫を表示する。作品を変更・削除・外部送信しないこと、初回は比較基準を作ることを説明する。実行中は割合を推定せず、確認対象と動作中を示すインジケータおよびcancel操作を表示する。 |
| FR-B10 tag | REQ-FR-B10-001, REQ-FR-B10-002, REQ-FR-B10-003, REQ-FR-B10-004 | FUT-C-022 | normalized tagのassign/remove/query/rename/merge、invalid拒否、migration、再起動保存を保証する。 |
| FR-B11 入力拡張 | REQ-FR-B11-001, REQ-FR-B11-002, REQ-FR-B11-003, REQ-FR-B11-004 | FUT-C-019, FUT-R-006, FUT-R-007 | catalogのopen・戻る・進む・上へ・更新・検索と、viewerのpage移動・表示・方向・倍率・ルーペ・全画面をkeyboard commandとしてremapし、command間および予約済みapp操作とのconflict拒否、個別/全体reset、focus fallback、再起動保存を保証する。viewerでは左右swipe、page layoutのwheel上下、右button+wheel上下、middle button、戻る/進むside buttonをcommandへ割り当て、double clickの全画面toggleは固定する。同じcommandへの複数mouse入力を許可し、旧keyboard/swipe設定を新しい既定値と統合して復元する。旧keyと新command既定値の衝突時は旧割り当てを優先し、新commandだけを未使用keyへ退避する。編集入口は統合設定だけに置き、group、command、keyboard、mouse、説明を同じcommand表で確認できるようにし、helpは現在のkeyboard割り当て表示だけを行う。原本変更・file削除・保存・印刷はglobal入力割り当てへ含めず、任意軌跡gesture、touch、gamepad、command parameterは候補のままとする。 |
| FR-B22 file manager | REQ-MVP-021, REQ-FR-B22-001, REQ-FR-B22-002, REQ-FR-B22-003, REQ-FR-B22-004 | FUT-C-024〜029, FUT-C-053 | 右click/keyboard context menu、Windows shell連携、選択項目のrename/move/copy/delete、現在folderへのcreate/paste、安全境界、成功後refreshを一貫して扱う。通常deleteはごみ箱、完全deleteは対象名を示す確認後だけ実行する。 |
| FR-B23 Leeyes viewer操作・外観 | REQ-FR-B23-001, REQ-FR-B23-002, REQ-FR-B23-003, REQ-FR-B23-004 | LEY-VIEWER-004, LEY-VIEWER-025, LEY-VIEWER-028 | page layoutの見開きを表示単位とは独立して1pageずつ前後へずらす。viewer背景、page周囲余白、見開き間隔、cursor自動非表示時間を統合設定、app-local設定、strict profileで検証・保存し、旧profileを安全な既定値へ移行する。 |
| FR-B24 アプリテーマ | REQ-FR-B24-001, REQ-FR-B24-002, REQ-FR-B24-003, REQ-FR-B24-004, REQ-FR-B24-005, REQ-FR-B24-006 | — | system追従と7つの検証済み組込み配色をshell全体へ適用し、利用者が安全なsemantic colorだけから最大32件のカスタムテーマを作成・複製・編集・削除・入出力できる。任意CSS、外部resource、画像、font、layout変更を受理せず、contrast、schema、size、名前をfrontendとRustで検証し、app-local SQLite、strict profile、named profile、native title barへatomicに接続する。 |

## 採用済みP1〜P10

| Priority / Feature | 対象ID | 現行契約 |
|---|---|---|
| P1 / FR-B13 | FUT-C-049, FUT-C-054, FUT-C-055, FUT-C-057, FUT-C-068 | refresh、複数・種別選択、相対path copy、property、現在位置status。 |
| P2 / FR-B14 | FUT-C-042, FUT-C-044, FUT-C-051, FUT-C-056 | root内open、成功openのrecent、履歴jump、明示終了。 |
| P3 / FR-B15 | FUT-C-045, FUT-C-046, FUT-C-047 | page-key bookmark、next/wrap、favoriteと別のapp-local bookshelf。 |
| P4 / FR-B16 | FUT-C-050, FUT-C-058 | 検索side paneのbasename maskと、absolute pathを含まずformulaを無害化するCSV出力。 |
| P5 / FR-B17 | FUT-C-065, FUT-C-066, FUT-C-067 | 5分類menu、accessible icon toolbar、情報カードとして永続化する既存`reference_tile`と、表紙専用`card_grid`。 |
| P6 / FR-B18 | FUT-C-060, FUT-C-061, FUT-C-062, FUT-C-063 | pane/bar可逆表示、viewer分離、native tray hide/showと終了の分離。 |
| P7 / FR-B19 | FUT-C-069, FUT-C-071, FUT-C-072, FUT-C-076, FUT-C-077 | atomic設定、strict profile、左右swipe gesture、offline help、version/runtime/license表示。統合設定は「一覧」「ビューワ」「画面とテーマ」「操作と入力」「プロファイル」の意味単位で移動でき、名前・説明・現在値を対象とする検索で該当設定だけを絞り込む。各設定は効果を説明し、全設定の既定値復元を含む変更は適用まで下書きに留める。統合設定と一般ヘルプの左navigationは選択中のアプリテーマに属する共通neutral面、本文側境界線、accentを薄く混ぜた現在地で階層と選択を示し、本文から独立した無関係な暗色sidebarを作らず、通常・hover・focus・selectedの文字とiconを識別可能にする。viewerのdouble clickは設定対象にせず全画面切替へ固定する。ヘルプmenuでは一般ヘルプとバージョン情報を別の項目・dialogとして開き、バージョン情報は製品version・runtime・third-party notice導線の内容量に合うcompactな専用dialogとして表示する。 |
| P8 / FR-B08 | FUT-C-006, FUT-C-007, FUT-C-008 | GIFの安全な分類・metadata・MIME・製品decodeと、AVIFの安全な分類・metadata・MIME・corrupt境界。AVIFの製品decodeは未受入。 |
| P9 / FR-B12 | FUT-C-001, FUT-C-002 | 単一volume・非暗号化RAR4/RAR5（RAR/CBR）、非暗号化7z（7z/CB7）、LHA/LZH（LZH）を安全に読み、分割RAR、暗号化書庫、未対応圧縮方式はunsupported分類する。一覧・検索結果・お気に入りでは、対応書庫の集合ではなく各項目の実際の形式（ZIP、CBZ、EPUB、RAR、CBR、7Z、CB7、LZH）を表示する。 |
| P10 / FR-B20 | FUT-C-073, FUT-C-074, FUT-C-075 | app-local thumbnail管理、明示保存、検証済みJPEG import。製品file picker gateは未完了。 |

## 全体受入シナリオ

- E2E-MVP-001: サイドバーのドライブ選択から画像folderを開き、単page/見開きで読み、再起動後にドライブと読書位置を復元する。起動時にlibrary root登録画面を表示せず、addressに`\\?\`を表示しない。
- E2E-MVP-002: ZIP/CBZ/EPUB/RAR/CBR/7z/CB7/LZHを閲覧してcacheと読書位置を生成しても、原本tree、hash、mtimeが一致する。
- E2E-MVP-003: catalogの自然順・sort順に従い、巻末から次の漫画の先頭または保存pageへ進む。
- E2E-MVP-004: network隔離状態で主要機能が動作し、外部DNS/TCP/UDP送信がないことを外部監視する。これは未完了gateである。
- E2E-MVP-005: catalogの右clickまたはkeyboard context menuからrename、copy、move、create、delete、OS clipboard pasteを行い、folder treeのfolderまたはdrive nodeからcut/copy/pasteを行う。選択した対象だけが変更され、アプリとWindows Explorerの双方向pasteおよび成功後のcatalog反映を確認する。
- E2E-MVP-006: 見開きを1pageずつ前後へずらし、背景・page周囲余白・見開き間隔・cursor自動非表示時間を変更して再起動およびprofile export/import後にも復元する。設定変更と閲覧の前後でlibrary原本を変更しない。
- E2E-MVP-007: system、全組込みテーマ、利用者が複製・編集したカスタムテーマを統合設定から適用し、shell、dialog、help、viewer chrome、native title bar、再起動、named profile、profile export/importで同じ配色を復元する。catalogも同じ全体テーマを継承し、viewer背景は指定どおり独立する。invalid・低contrast・過大・未知fieldを含むテーマは保存・適用されない。

## Leeyes P1-A 即効改善の受入条件

Leeyesの観察可能な操作目的を独自実装し、旧UI、文章、画像、iconはコピーしない。設定変更は
app-local SQLiteとstrict profileへ保存し、library原本を変更しない。

| 要件ID | Leeyes ID | 受入条件 |
|---|---|---|
| REQ-LEY-P1-001 | LEY-SHELL-012 | 統合設定からmenu bar、navigation toolbar、address bar、status barを個別に表示・非表示へ切り替え、再起動とprofile export/import後に復元する。すべてを隠しても設定画面を再度開けるkeyboard入口を残す。 |
| REQ-LEY-P1-002 | LEY-SHELL-013 | 「常に手前」を統合設定から切り替え、main windowへだけ適用する。native window APIが失敗した場合は保存済み状態と成功表示を更新せず分類した局所errorを示す。再起動とprofileで復元する。 |
| REQ-LEY-P1-003 | LEY-VIEWER-020 | 任意倍率を1%〜800%の整数として直接入力し、範囲外と非数値を拒否する。fit、原寸、段階zoom、表示倍率表示と同じscale modelを使い、巨大なdecodeや原本変更を発生させない。 |
| REQ-LEY-P1-004 | LEY-VIEWER-022 | zoom保持を`global`、`book`、`page`から選ぶ。globalは次の作品と再起動へ保存し、bookは現在作品を閉じるまで、pageは現在pageを離れるまで保持する。fit系modeと任意倍率を同じpolicyで扱う。 |
| REQ-LEY-P1-005 | LEY-INPUT-004 | shortcut設定はcommandごとの既定値復元と全commandの既定値復元を区別し、いずれも予約key・重複を生じない。変更は設定dialogのdraftに留め、適用またはcancelの既存atomic境界を守る。 |
| REQ-LEY-P1-006 | LEY-VIEWER-021 | 現在の先頭表示画像について幅または高さを1〜32768pxで指定し、実画像寸法と縦横比から安全な1%〜800%の表示scaleへ換算する。自然寸法が未取得、0、範囲外、または換算scaleが範囲外なら適用しない。 |
| REQ-LEY-P1-007 | LEY-VIEWER-031 | viewerへ非破壊grid overlayを表示し、無効または8〜256pxの間隔と明色・暗色を選べる。overlayはpointer操作、画像decode、clipboard、thumbnail、原本に影響せず、設定を再起動とprofileで復元する。 |
| REQ-LEY-P1-008 | LEY-INPUT-010 | viewerのpointer pan係数を50%〜200%で設定し、pointer capture中の移動量へだけ適用する。swipe判定、click、drag終了、keyboard操作へは適用しない。 |
| REQ-LEY-P1-009 | LEY-INPUT-012 | ページ送りviewerのwheel入力に0〜200の不感帯を設定し、閾値未満のdeltaはpage commandへ変換しない。Ctrl+wheelと右button+wheelの既存境界を維持する。 |

## Leeyes P1-B 既存機能完成の受入条件

| 要件ID | Leeyes ID | 受入条件 |
|---|---|---|
| REQ-LEY-P1-010 | LEY-FILER-016 | folder移動後の初期選択を「選択なし」「先頭」「末尾」「前回選択を復元」から選べる。表示中sort/filter後の項目へ適用し、空folderでは選択せず、検索結果から戻る際の明示的な対象復元を優先する。設定をprofileと再起動で復元する。 |
| REQ-LEY-P1-011 | LEY-CATALOG-010 | 既存の一覧形式別thumbnail寸法に加え、生成範囲を「表示中のみ」「表示中と近傍」「全項目」から選べる。設定変更後は要求queueを再評価し、同一画像生成物のcache keyを不要に変えず、worker上限とcache上限を維持する。profileと再起動で復元する。 |
| REQ-LEY-P1-012 | LEY-FILE-001 | Windows標準file dialogを対応画像・書庫・PDFのfilter付きで開き、cancelを正常終了として扱う。選択fileをcanonicalizeし、対応形式・通常fileであることをbackendで検証してから、そのdriveを安全境界として登録し直接viewerへ開く。root外path文字列を既存rootへ注入せず、失敗時は現在画面を保持する。 |
| REQ-LEY-P1-013 | LEY-FILE-009 | 成功して開いた最近の作品を新しい順・重複なし・最大20件で表示し、項目から再度開ける。missing・access拒否は局所errorとして履歴画面を維持し、全履歴を明示的に消去できる。cancel・失敗openは追加しない。 |
| REQ-LEY-P1-014 | LEY-SETTING-005 | 起動場所を「前回のfolder」「前回driveのroot」から選び、存在しない・読めない保存pathではshellを維持して安全にdrive選択へ戻す。初期選択はREQ-LEY-P1-010のpolicyを共有し、設定をstrict profileとapp-local SQLiteへ保存する。 |
| REQ-LEY-P1-015 | LEY-HELP-001 | 同梱dataだけで閲覧できる利用者向けoffline helpへ、開始、folder/list、viewer、検索、file操作、設定、privacy・安全上の制約、現在のshortcutを章立てして収録する。helpはheader、章navigation、選択中の記事、固定footerからなる2 pane構成とし、章を選ぶと対応する記事だけを表示する。検索は章title・本文・操作名・現在のshortcut割り当てを横断し、一致する章とshortcutを結果として示す。狭いwindowでは1 columnへ縮退し、現在地、検索結果0件、検索clear、閉じる操作をkeyboardと支援技術から識別できる。shortcutはread-only表示とし、外部URLやnetworkを要求しない。 |

## Leeyes P1-C 独立S機能の受入条件

| Requirement | Leeyes ID | 受入条件 |
|---|---|---|
| REQ-LEY-P1-016 | LEY-FILER-007 | File menuからWindowsのDesktop、Downloads、Documents、Picturesへ移動できる。known-folder APIが返した実filesystem pathだけを使い、通常のdrive登録・canonical containment境界を経由する。取得不能な場所は表示せず、shell virtual extensionや任意codeを読み込まない。PCは既存tree入口を正とする。 |
| REQ-LEY-P1-017 | LEY-FILER-011 | Windows hidden属性または先頭dotを持つ項目を既定で一覧・folder page列挙から除外し、「隠し項目を表示」で一覧に含められる。設定は再起動とprofileで復元し、symlink/reparse point、root containment、対応形式の既存安全境界を変更しない。 |
| REQ-LEY-P1-018 | LEY-FILER-014 | catalogにkeyboard focusがあるとき、IME composition外の表示可能文字を1秒以内に続けて入力すると、NFKC・case insensitiveな名前前方一致で次の項目へ選択とfocusを移す。同じ文字列の再入力は現在位置の次から循環し、input・dialog・viewerのshortcutを奪わない。 |
| REQ-LEY-P1-019 | LEY-FILER-018 | catalogは全体テーマを継承し、一覧だけを別配色にする重複設定は提供しない。旧profile・named profile・app-local設定にある一覧配色値は読込み時に破棄し、原本・thumbnailを加工しない。 |
| REQ-LEY-P1-020 | LEY-VIEWER-006 | 2page以上のviewerで現在page以外を一様に選ぶrandom移動commandを提供し、1pageでは何も変更しない。page sequence、読書位置保存、prefetch上限、原本を変更せず、選択結果を通常page移動と同じ境界へ渡す。 |
| REQ-LEY-P1-021 | LEY-SETTING-006 | 「前回の画像を再表示」を明示的に有効化した場合だけ、前回root復元後に最新の成功した閲覧作品と保存pageを再度開く。既定は無効とし、cancel・失敗openを対象にせず、missing・access拒否・removable drive不在ではshellを維持して局所errorを表示する。設定はprofileとSQLiteへ保存する。 |

## Leeyes P2 閲覧中核の受入条件

| Requirement | Leeyes ID | 受入条件 |
|---|---|---|
| REQ-LEY-P2-001 | LEY-VIEWER-007 | 2page以上のviewerはtoolbarからslideshowを開始・停止でき、slideshow起動commandでは開始状態で開く。既定間隔3秒で通常の次page commandを1回ずつ実行し、page decode/prefetch待機と巻末policyを迂回しない。documentが非表示またはwindow focusを失った間は自動送りせず、復帰後に新しい1 intervalを待つ。1pageでは開始不可とし、停止・viewer終了・unmountでtimerを破棄する。詳細な間隔・順序・反復・random設定はLEY-VIEWER-008で要件化し、このbatchでは固定しない。 |
| REQ-LEY-P2-002 | LEY-VIEWER-009 | 巻末動作は「次巻を自動」「次巻を確認」「libraryへ戻る」「停止」「最終巻から先頭巻へloop」の5 policyを設定dialogとviewerで選択・永続化する。archive、PDF、画像folderを同じ読み取り可能volumeとして現在のcatalog sort順で扱い、次巻は先頭page、前巻は末尾pageから開く。「libraryへ戻る」は次巻の有無にかかわらず巻末でviewerを閉じる。次巻なし、現在volume不明、一覧再取得失敗では安全に停止し、確認前・失敗時に別volumeを開かない。 |
| REQ-LEY-P2-003 | LEY-VIEWER-010 | 作品ごとに複数の任意pageをしおりとしてapp-local SQLiteへ保存し、同じpageの再保存は重複を作らず最新ordinal hintへ更新する。viewerを開くと保存済みしおりを読込み、現在のpageKey列へ再解決して次の有効なしおりへ循環移動できる。欠落pageは一覧に残して無効と明示し、個別削除できる。既存root-scoped localStorageしおりは対象作品を初めて開いたときSQLiteへ重複なく移行し、全件成功後だけ旧行を除去する。root外path、空key、不正ordinal・時刻、過大な移行payloadは拒否し、失敗時はviewerと旧データを保持して局所errorを表示する。 |
| REQ-LEY-P2-004 | LEY-VIEWER-013 | viewerの表示枚数を「自動」「単ページ」「見開き」から明示選択し、toolbarと設定profile/SQLiteで同じmodeを保持する。単ページは常に1page、見開きは横長pageと最終余りpageを単独表示し、それ以外を2page単位で表示する。自動はpage layoutかつ表示領域の幅/高さ比が1.25以上のときだけ2枚の縦長pageを見開きにし、狭い領域、横長page、最終余りpageでは単独表示する。resizeで自動判定を更新しても現在anchorを保持し、次移動は現在の表示枚数、前移動は実際に通ったanchor履歴へ戻る。詳細なしきい値、先頭単独、偶奇条件はLEY-VIEWER-014へ分離する。 |
| REQ-LEY-P2-005 | LEY-VIEWER-014 | 見開き条件として、縦長pageとみなす最大幅/高さ比を50〜100%、自動見開きを許可するviewer領域の最小幅/高さ比を100〜300%、先頭pageの単独表示を有効/無効、組合せ開始を「制限なし」「奇数page」「偶数page」から選択できる。見開き候補は現在pageと次pageの両方が縦長条件を満たし、選択した開始page条件を満たす場合だけ2pageとする。単独page、横長page、条件外pageの後は選択した偶奇条件へ再同期し、最終余りpageは単独とする。設定dialog、profile v8、app-local SQLite、通常viewerを同じ値へ接続し、旧profile v1〜v7と欠落DB keyはP2-D相当の既定値（100%、125%、先頭単独なし、制限なし）へ安全に移行する。不正値はprofile import/native境界で拒否し、原本へ書き込まない。 |
| REQ-LEY-P2-006 | LEY-VIEWER-019 | 全体フィットの詳細として、小画像の拡大を許可/縮小のみ、見開き全体を1領域へ収める/各pageを1領域基準で合わせる、page周囲余白をfit計算へ含める/含めないを選択できる。ページ送りviewerの全体フィットでは、読込み済みpageのnatural寸法、viewer領域、見開き間隔、選択した余白基準から共通倍率を計算し、縦横比を維持して両pageへ同じ倍率を適用する。見開き全体基準は2page幅とgapを合わせて領域内へ収め、page基準は各page単独の最大寸法を基準にして必要ならscroll/panを許す。縮小のみでは倍率を100%以下、拡大許可でも既存の1〜800%安全範囲に制限する。画像寸法が未確定・0・非有限なら従来CSS fitへ安全にfallbackし、width/height/original/custom modeを維持する。設定dialog、profile v9、app-local SQLite、通常viewerを接続し、旧profile v1〜v8と欠落DB keyは「縮小のみ・見開き全体・余白を含む」へ移行する。 |
| REQ-LEY-P2-007 | LEY-VIEWER-026 | ページ送りviewerで表示領域を超える画像のscroll/pan詳細として、page移動commandで送る量をviewport高の10〜100%、page内移動の滑らかなanimationを有効/無効から設定できる。next commandは下端へ達するまで設定量ずつ下へ送り、previous commandは上端へ達するまで同じ量ずつ上へ戻し、端に達した後だけ隣page/作品へ移動する。smooth有効時もOSの`prefers-reduced-motion`が有効なら即時移動を優先する。pointer dragは既存の50〜200% pan係数とpointer captureを維持してrelease後の慣性移動を発生させない。Ctrl+wheel、右button+wheel、wheel不感帯、swipe、N字/Z字走査は変更しない。設定dialog、profile v10、app-local SQLite、通常viewerを接続し、旧profile v1〜v9と欠落DB keyは90%・smooth有効へ移行する。不正値はprofile import/native境界で拒否し、原本へ書き込まない。 |
| REQ-LEY-P2-008 | LEY-VIEWER-027 | ページ送りviewerで表示領域を縦横とも超える画像・見開きを、標準縦送り、N字、Z字から選んだ規則で読書順に走査する。N字は設定済みpage内scroll量で上から下へ進み、下端で読書方向の次列へ移って上端へ戻るcolumn優先とする。Z字は読書方向へ横に進み、行端で下の次行へ移って読書方向の先頭端へ戻るrow優先とする。右開きは右端から左へ、左開きは左端から右へ進む。previousは各経路を厳密に逆順で戻り、経路の先頭/末尾へ達した後だけ前後page・作品へ移動する。各stepはviewport幅/高さにREQ-LEY-P2-007の10〜100%量を適用し、行/列の切替は1回のatomic scrollとして対角線上の中間frameを作らない。smooth/reduced-motion、pointer pan、fit、見開き、巻末policyを既存境界のまま利用する。設定dialog、profile v11、app-local SQLite、通常viewerを接続し、旧profile v1〜v10と欠落DB keyは標準縦送りへ移行する。不正値はprofile import/native境界で拒否し、原本へ書き込まない。 |
| REQ-LEY-P2-009 | LEY-VIEWER-029 | 既存の非破壊pointerルーペについて、正方形の表示サイズを80〜400pxの整数、拡大率を125〜800%で設定できる。ルーペは有効時に読込み済みの表示page上へpointerがある間だけ表示し、元画像やcacheを変更せず、現在の表示画像をCSS backgroundとして再利用する。pointerの画像内座標はpage境界へ、ルーペ中心はviewer stage内へclampし、stageが指定サイズより小さい場合はstage中央へ置いて画面外への拡大を抑える。page移動、倍率変更、pointer leave、ルーペ無効化では即座に消し、cursor自動非表示とpointer panの既存抑止を維持する。設定dialog、profile v12、app-local SQLite、通常viewerを接続し、旧profile v1〜v11と欠落DB keyは180px・200%へ移行する。frontend/native境界で範囲外・非有限値を拒否し、GPU負荷を制限する。 |
| REQ-LEY-P2-010 | LEY-VIEWER-032 | viewerの先読み量として現在の表示単位より後を0〜4page、前を0〜4page、media grantに保持する圧縮済みpage byteの上限を16〜512MiBから選択できる。既定値は既存挙動と同じ後4page・前0page・256MiBとし、ページ送りviewerは現在anchorを起点とするbounded windowだけを要求する。0page指定では該当方向をbackground要求せず、利用者が未先読みpageへ移動した時点でvisible優先度として読込みを開始する。見開き遷移は設定上限に収まる場合は表示単位全体を先読みし、未準備pageがある場合は現在の見開きを保持して全pageのloadまたは局所error確定後だけ原子的に切り替える。backendのmedia registryは期限切れgrantを除去したうえでLRU順に非表示pageのgrantを解放し、上限を超える単一pageはその1件だけを許容して無制限な蓄積を防ぐ。window外へ出たfrontend media URI・decode状態・失敗状態を解放し、stale generation結果は復帰させない。設定dialog、profile v13、app-local SQLite、通常viewer、native media registryを接続し、旧profile v1〜v12と欠落DB keyは既定値へ移行する。範囲外・非整数・非有限値はfrontend/native境界で拒否し、原本、thumbnail cache、任意code実行境界を変更しない。 |
| REQ-LEY-P2-011 | LEY-VIEWER-033 | 全画面中の終了操作を「Escで全画面だけ解除」または「Escで全画面を解除してviewerも閉じる」から選択し、後者でもnative全画面を解除できた後だけviewerを閉じる。全画面中だけWindowsへdisplay-required要求を発行してスクリーンセーバーと画面消灯を抑止する設定を有効/無効から選択でき、既定は既存挙動を保つため全画面だけ解除・抑止無効とする。抑止要求は全画面遷移成功後に取得し、全画面解除、viewer終了、unmount、application終了で必ず解放する。取得失敗時は全画面開始をrollbackし、解除失敗時は抑止を再取得して状態を一致させ、局所errorを表示する。OS全体の電源設定は変更せず、handle-scoped Windows power request APIだけを使い、非Windows buildは明示的な非対応errorとする。設定dialog、profile v14、app-local SQLite、通常viewer、native commandを接続し、旧profile v1〜v13と欠落DB keyは既定値へ移行する。 |
| REQ-LEY-P2-012 | LEY-SHELL-014 | task trayへの格納条件として「手動のみ」へ加えて最小化時の自動格納を有効/無効、main windowの閉じる操作を「アプリを終了」または「task trayへ格納」から選択できる。tray iconからの復帰は左single clickまたは左double clickを選択し、いずれもwindowをshow・unminimize・focusの順で1回だけ復帰する。既定値は既存挙動を保つため自動格納無効・閉じると終了・single click復帰とする。close-to-tray設定中の閉じる操作は明示Quitと区別し、先にcloseをpreventしてから格納するため、tray unavailable・hide失敗でも誤ってprocessを終了せずwindowを表示可能な状態へ戻す。最小化時はtrayが利用可能な場合だけ重複格納せずhideし、失敗時は通常の最小化状態を保持する。File menuとtray menuの明示Quitは設定にかかわらずapp-local stateをflushして終了する。設定dialog、profile v15、app-local SQLite、native window event loop、tray icon eventを接続し、旧profile v1〜v14と欠落DB keyは既定値へ移行する。 |
| REQ-LEY-P2-013 | LEY-VIEWER-008 | slideshowの間隔を0.5〜60.0秒、順序を順方向・逆方向・random、反復を「現在の作品を繰り返す」の有効/無効から設定し、設定dialog、profile v16、app-local SQLite、通常viewerとslideshow起動commandで共有する。既定値は既存挙動を保つ3秒・順方向・反復無効とし、旧profile v1〜v15と欠落DB keyをこの値へ移行、不正値はimport/native境界で拒否する。順方向・逆方向は反復無効なら既存の前後移動と巻末/巻頭policyを使い、反復有効なら作品境界で同一作品の反対端へ移動して外部作品へ遷移しない。randomは現在pageを除く全pageを1 cycle内で重複なくshuffleし、反復無効なら1 cycle完了時に停止、反復有効なら直前pageと異なる新cycleを開始する。設定変更、手動移動、表示単位変更、復帰時は古いtimerを破棄して新しい1 intervalを待ち、background・focus喪失中は停止する。page decode/prefetch待機、読書位置保存、1page無効化、viewer終了時cleanupの既存境界を迂回せず、同時timerは1件、random queueはpage数以下に制限する。Leeyesのrandom重複規則は未確認のため、予測可能性を高める安全な独自shuffle-bagとして実装した仮定を記録する。 |
| REQ-LEY-P2-014 | LEY-VIEWER-011 | viewer toolbarから現在のanchor pageをWindows clipboardへ画像データとしてコピーし、成功時はpage番号・pixel寸法を、失敗時は再試行可能な理由をviewer内statusへ表示する。見開きでは合成せず現在pageだけを対象とし、fit・zoom・grid・loupe等の表示overlayは含めない。原本を変更せず、folder・archive・PDF・対応画像形式を既存のcontained path、archive展開上限、PDF render、page decodeと同じRust境界で読み、EXIF向きを適用した32bpp BGRAをCF_DIBV5へ書く。透明度は対応形式で保持し、animated形式は既存の先頭frame境界を使う。sourceは256MiB、長辺16,384px、120,000,000pixelの既存上限に加え、header込みclipboard payloadを256MiB以下に制限し、checked arithmetic後にだけallocateする。clipboard所有権は`SetClipboardData`成功時だけOSへ移し、decode・allocation・lock失敗時は`EmptyClipboard`前に停止し、write失敗は明示errorにする。重複操作中はbuttonを無効化し、viewer/page/session変更後の古い結果を表示しない。Rust native commandを中核とし、TypeScriptは現在pageの識別と状態表示だけを担う。release製品で他appへ貼り付ける直接観測がない場合は未測定として記録する。 |
| REQ-LEY-P2-015 | LEY-VIEWER-012 | Viewerのanchor page移動に合わせて背後のcatalog選択と復帰時のscroll位置を同期する設定を設け、既定は有効とする。画像folderを閲覧中で現在pageが読み込み済みcatalogに存在する場合は、そのpageだけをactive selectionとselection anchorにする。archive・PDF・comic folder等でpage自体がcatalog項目でない場合は、現在作品のitem keyがcatalogに存在するときその作品を選択する。次巻・前巻移動では新しいviewer sessionのitem keyへ同期する。同期無効時はviewer開始前のcatalog選択を変えず、検索・mask・別folderにより候補が現在のvisible catalogに存在しない場合も選択やnavigationを強制変更しない。Viewerを閉じてcatalogが再mountされたときは既存virtualizerの選択scroll契約で同期対象を表示し、page callbackは現行viewer sessionとgenerationに一致する場合だけ反映する。設定はstrict profile v17とapp-local SQLiteへatomicに保存し、profile v1〜v16およびkeyがない既存SQLiteには有効を補う。原本・読書位置・bookmarkは変更せず、同期処理にnative I/O、polling、page数比例の追加stateを導入しない。release製品で大量catalogの復帰scrollを直接観測していない場合は未測定として記録する。 |
| REQ-LEY-P2-016 | LEY-VIEWER-030 | Viewer toolbarと固定keyboard操作から、現在のanchor pageだけへ時計回り90度回転、左右反転、上下反転、変換resetを適用する。keyboardは入力欄・select・contenteditable・dialog操作中には発火せず、`]`、`H`、`V`、`0`をそれぞれ割り当ててbutton titleでも説明する。変換はpageごとのsession一時stateとして合成し、pageを離れて戻った場合は同一session内だけ保持し、次巻・前巻を含む新sessionまたはViewer終了で破棄する。左右・上下は画面上の軸を意味し、見開きでも隣pageや原本へ波及しない。EXIF補正済みmediaをCSSで非破壊変換し、media URI、decode/prefetch cache、読書位置、bookmark、画像clipboardの原データは変更しない。90/270度では幅・高さを入れ替えてauto見開き判定と全体fit倍率を再計算し、ページ送りの既存overflow/pan境界を維持する。loupeは現在pageと同じ回転・反転を同じ表示座標で描画し、gridは画面固定overlayのままとする。stateは変換したpage数以下、操作は現在pageのO(1)更新とし、canvas再decode、native I/O、timerを追加しない。helper、Viewer、CSS contractを自動検証し、release WebView2の実画像・見開き・DPI・GPU描画を直接観測していない場合は未測定として記録する。 |

## Leeyes P3 操作・検索の受入条件

| Requirement | Leeyes ID | 受入条件 |
|---|---|---|
| REQ-LEY-P3-001 | LEY-SEARCH-003 | 名前検索は従来の空白を含むplain文字列のcase・全半角英数非依存部分一致を維持しつつ、`*`（0文字以上）と`?`（任意1文字）のwildcard term、case-insensitiveな`AND`・`OR`・`NOT`、丸括弧を受理する。優先順位は括弧、NOT、AND、ORとし、論理演算子はtoken境界だけで認識する。quoted termは空白と演算子文字列をliteralとして部分一致し、backslashは次の特殊文字をescapeする。wildcard termはbasename全体へ、plain/quoted termはbasename内へ適用し、検索scope・kind・size・date・固定場所条件とはANDで組み合わせる。空式、dangling operator、unclosed quote/parenthesis、unexpected token、1024文字超、128 token超、16階層超をfilesystem走査前に`INVALID_REQUEST`として拒否し、UIは構文例と修正可能なerrorを表示する。評価は短絡し、termごとのregex engineやbacktrackingを使わず、basename長×式token数の有界な独自matcherとする。Leeyesの厳密な優先順位・escapeは未確認のため、この明示構文を安全で予測可能な現代化として採用し、原本、検索場所、index/cacheを変更しない。 |
| REQ-LEY-P3-002 | LEY-CATALOG-006 | catalogには「現在の一覧を絞り込む」filter bar、適用条件、保存済み一覧filterを表示せず、列挙済みの現在一覧を追加条件で絞り込まない。名前による絞り込みは検索side paneへ一本化し、検索を閉じると現在folderの全catalog項目へ戻る。旧版で保存した一覧filterはcatalog表示へ適用しない。 |
| REQ-LEY-P3-003 | LEY-CATALOG-007 | catalog固有の詳細filterと保存済み条件のUIは提供しない。folder/file、size、更新日の条件指定は名前検索の詳細条件へ一本化し、旧版のapp-local SQLiteに保存されたcatalog maskは保持してもcatalogへ復元・適用しない。 |
| REQ-LEY-P3-004 | LEY-SEARCH-001, LEY-SEARCH-002 | 名前検索の範囲を「現在のフォルダー」「現在のライブラリ全体」「指定した複数の場所」の単一選択として表示する。現在folderは検索実行時のcurrent relative pathを固定場所として渡し、library全体はcurrent rootから、複数場所は現在libraryとWindows標準folder pickerで利用者が明示選択した読み取り可能なfolderをsession中最大8件まで入力順に横断する。複数場所を選んだ場合だけ追加source一覧と追加・削除操作を表示し、追加sourceがない場合も現在libraryを最低1 sourceとして扱う。picker cancelは正常終了で現在sourceを保持し、任意のfrontend文字列は検索sourceとして受理せず、Rustがcanonicalizeしてsession allowlistへ登録したpathと現在library rootだけを許可する。sourceはcanonical pathのcase-insensitive一致で重複排除し、0件を拒否する。複数source時はcurrent folder固定条件を無効化して各source rootから同じquery・subfolder・kind・size・date条件を適用し、重なるsourceの同一canonical itemは先に指定したsourceの結果1件へ統合する。全source合計50,000結果で`RESOURCE_LIMIT`とし、cancel/stale generationを既存境界で破棄する。結果はsource rootとsource内relative pathを分離して表示し、同名でもsourceが違えば区別する。結果を開くと選択したsourceを新しいlibrary rootとしてbackendで再検証・登録して親folderへ移動し、検索前rootへ文字列を注入しない。source追加・削除はsession限定でprofile/SQLiteへ保存せず、library原本、index、cacheを変更しない。Leeyesのsource種別・重複優先順位は未確認のため、明示選択folderをsource、入力順優先のcanonical重複排除を安全な現代化として採用する。2つ以上の実filesystem source、重なり、missing/access、50,000上限、cancelをRust testで検証し、release folder pickerとslow/removable driveを未観測なら未測定と記録する。 |
| REQ-LEY-P3-005 | LEY-FILER-010 | 既定有効の「現在フォルダーを自動更新」を統合設定とstrict profile v18、app-local SQLiteへ保存し、profile v1〜v17と既存DBには有効を補う。有効時はRustが現在library root内の表示中canonical folderだけへ非再帰のOS filesystem watcherを最大1件設定し、作成・削除・rename・内容/metadata変更を250ms以上の間隔でcoalesceしたgeneration付きeventとして通知する。frontendは現在root・navigation path・watch generationが一致し、catalogが通常表示中のeventだけを既存`list_folder`境界による再列挙へ変換し、更新前selectionのうち残存項目を維持して欠落項目だけを解除する。root/folder移動は旧watcherを置換し、設定無効化、root解除、application shutdownではwatcherをdropする。watch登録失敗、監視後のmissing/access、event stormはshellを停止させず、局所statusと手動F5を残し、無限retryや原本修復を行わない。watcherは書庫内部、下位folder、network全体、cache、検索sourceを監視せず、library原本・index・cacheを変更しない。Leeyesのdebounce、既定値、監視深度は未確認のため、既定有効、250ms coalescing、表示中folderだけの1 watcherをboundedな現代化として採用する。Rust testで実temp folderの作成・rename・削除通知、path containment、watcher置換/drop、coalescing上限を検証し、frontend testでstale/別path/無効中のevent破棄とselection保持を検証する。release WebView2、network/removable drive、10,000-event burst、基準PCの反映時間・CPU・working setを未観測なら未測定と記録する。 |
| REQ-LEY-P3-006 | LEY-FILER-015 | folder treeの詳細動作として「現在位置以外を自動的に折りたたむ」「直下folderに下位folderがあるか確認する」、tree幅180〜480pxを統合設定、strict profile v19、app-local SQLiteへ保存し、profile v1〜v18と既存DBには自動折畳み無効・下位確認有効・幅240pxを補う。Rust `list_tree_children`はcanonical library root内のdirect child folderだけを返し、下位確認有効時は各childを非再帰・先頭一致で調べた`hasChildren`を付与する。root外、reparse point、access/missing/errorは既存の局所error境界を維持し、下位確認無効時は未確認としてexpanderを残す。frontendは`hasChildren=false`のleafを展開不可にし、自動折畳み有効時のcurrent path変更ではPC・active drive・current ancestor chainだけを残して別branchを閉じる。幅はpointerとkeyboardの両方で同じ範囲へclampし、設定適用・再起動・profile importで復元する。書庫をtree nodeとして列挙する挙動はLEY-FILER-002、書庫内階層はLEY-FILER-003のP4範囲であり、この要件では追加しない。原本、archive、cacheを変更せず、全treeを再帰走査しない。Rust testでdirect child、hidden設定、leaf/branch判定、containmentを、frontend testでleaf expander、自動折畳み、幅のpointer/keyboard clampとprofile移行を検証する。10,000 direct foldersは時間を測定し、release WebView2のremote/removable drive、access変化、DPI別drag、CPU・working setを未観測なら未測定と記録する。 |
| REQ-LEY-P3-007 | LEY-FILER-017 | catalog項目のdouble clickとmodifierなしEnterについて、folder/comic folderは「移動」「viewerで読む」「何もしない」、画像は「viewerで読む」「何もしない」、書庫/PDFは「viewerで読む」「何もしない」から対象種別ごとに規則を選び、統合設定、strict profile v20、app-local SQLiteへ保存する。profile v1〜v19と既存DBには現行挙動を保つfolder移動・画像読書・書庫/PDF読書を補う。TypeScriptはgesture、対象kind、generationをRust `resolve_catalog_activation`へ渡し、Rustが保存済みenumとtyped `ItemKind`から`navigate`、`read`、`none`のいずれかを決定した後だけ既存navigation/open portへdispatchする。stale/cancel/errorでは項目選択とcatalogを保持し、操作を推測実行しない。Ctrl+Enterは既存の明示的な強制読書shortcutとして画像・書庫・PDFだけを`read`へ解決し、folder設定や通常activationを迂回してfolderを開かない。unsupported fileは常に`none`とし、外部app起動、書庫tree移動、single clickでの即時open、新しいfilesystem権限を追加しない。folderの「読む」が画像を含まない場合は既存open errorを局所表示し、navigateへ暗黙fallbackしない。設定値はfrontend/native双方でstrictに検証し、原本、cache、履歴をactivation成功前に変更しない。Rust table testで全kind・rule・triggerを、frontend testでdouble click/Enter/Ctrl+Enter、none、stale/error、profile移行とSQLite再openを検証する。release WebView2の実double click、IME/keyboard、removable drive、activation latencyを未観測なら未測定と記録する。 |
| REQ-LEY-P3-008 | LEY-FILER-019 | 詳細リストについて、行区切りを「なし」「横」「縦横」、行密度を「コンパクト」「標準」「ゆったり」から選び、必須の名前列に加えて種別・サイズ・更新日時列を個別に表示/非表示へ切り替えられるようにする。既定は現行互換の区切りなし・標準54px・全列表示とし、統合設定、strict profile v21、app-local SQLiteへatomicに保存する。profile v1〜v20と既存DBにはこの既定値を補い、frontend/native双方でenumとbooleanをstrictに検証する。headerと各行は同じ可視列templateを共有し、狭幅では更新日時、次に種別・サイズを既存優先順で縮退する。非表示列も項目buttonのaccessible nameには残し、名前列、favorite、selection、keyboard、incremental search、sort、virtualization、他4一覧形式を変更しない。罫線はCSSだけで描画してselection/focus borderと競合させず、原本、metadata、cacheへ書き込まない。Rustでprofile/SQLite移行・拒否・再openを、frontendで全組合せのheader/row対応、密度、罫線data contract、狭幅style、設定保存を検証する。10,000項目は既存virtualizationを維持し、行設定による全件DOM展開や項目単位stateを追加しない。release WebView2のDPI、high contrast、font scaling、10,000実項目のFPS・working setを未観測なら未測定と記録する。 |
| REQ-LEY-P3-009 | LEY-CATALOG-016 | サムネイル管理から「現在folder以下」または「library全体」を明示選択し、対象pathと上限を確認してから、folderの直下画像cover、対応画像、書庫、PDFのサムネイルを再帰的にapp-local cacheへ一括生成する。Rustはlibrary rootと開始folderをcanonicalize・containment検証し、hidden設定を既存catalogと共有し、symlink/reparse pointを再帰せず、深さ64、走査50,000項目、生成候補10,000件を生成開始前に検証する。対象候補は自然順で固定し、unsupported fileと直下画像のないfolderは生成対象から除外する。生成は既存の2-worker bounded priority queueへ1件ずつbackground priorityで投入してvisible/near要求を優先可能にし、既存`ThumbnailPipeline`のfingerprint、negative cache、atomic write、LRU、hard capを迂回しない。独立したgeneration/cancellationを用い、新しい一括生成、library root変更、明示cancel、shutdownで旧処理を停止し、staleなprogress/resultをUIへ反映しない。UIは列挙中と、総数・処理済み・新規生成・cache hit・失敗をgeneration付きeventで表示し、実行中の二重開始を防ぎ、個別の破損・access失敗は件数へ集約して残りを続行する。cancelは次の候補投入前または実行中1件の完了後に停止し、完了済みcacheは安全な再利用物として保持する。処理はlibrary原本、metadata、利用者読込thumbnail、選択、現在folderを変更せず、任意code、外部app、書庫の展開保存を行わない。Leeyesの厳密な範囲・上限・progress表現は未確認のため、この明示範囲、事前上限、cancel可能なbounded処理を安全な現代化として採用する。Rust testでcontainment、hidden、symlink、自然順、上限、cancel、cache hit/失敗継続を、frontend testで範囲payload、progress、二重開始、cancel、stale/root変更破棄を検証する。10,000候補列挙と少数実画像生成の時間を測定し、releaseの10,000実画像、巨大画像、書庫/PDF混在、slow/removable drive、CPU・working set・cache eviction時間を未観測なら未測定と記録する。 |
| REQ-LEY-P3-010 | LEY-FILE-020 | catalogまたはfolder treeのlibrary内項目を同じdriveのfolderへdrag and dropした場合、modifierなしは既存互換のmove、Ctrl付きはcopyとして扱い、選択中の複数項目を1回のRust file-operation境界へ渡す。Rustはsourceとdestinationを毎回canonicalizeして現在library rootへの包含、重複、missing/access、reparse point、同名衝突、folder自身・子孫へのtransferを実行直前にも検証し、失敗を局所通知して再列挙する。Windows Explorer等から受けたnative file-drop payloadは最大256個の絶対pathに限定し、Rustがfile/folder、canonical path、reparse point、重複、宛先衝突と子孫関係をread-only previewで検証した後、項目数・名前・copy先を示す確認dialogで利用者が承認した場合だけlibrary内のdrop targetへcopyする。外部dropは原本をmove/deleteせず、cancel、stale generation、rootまたは宛先変更、preview後のfilesystem変化では実行しないか再検証して失敗する。library内の選択項目をAlt付きでdrag開始すると、Rustがroot包含を再検証してWindows Shellのnative file payloadをcopy-onlyで開始し、Explorer側のdrop cancelでは原本を変更しない。frontendはpointer座標から既存の明示drop targetとmodifierを選び、native pathの解釈、filesystem走査、copy/move、Shell data object生成を行わない。Leeyesのmodifierとtarget別全行列は未確認であり、外部dropによる暗黙open、library root登録、外部move、上書き、任意app起動は採用せず、明示確認付きimport copy、library内move/copy、copy-only drag-outを安全な現代化として採用する。Rust testで内部copy/move、外部preview/executeの絶対path・256件上限・重複・衝突・reparse・self/descendant・preview後変化、Shell payload source包含を、frontend testでtarget解決、Ctrl copy、既定move、Alt drag-out、confirm/cancel/stale/root変更を検証する。release WebView2とExplorer間の実drag-in/out、DPI別pointer座標、network/removable drive、大規模folder copyの時間・CPU・working setは未観測なら未測定と記録する。 |

| REQ-LEY-P3-011 | LEY-INPUT-001 | catalog・viewerの各既知commandへ1〜4個のkeyboard bindingを順序付きで割り当て、追加・削除・既存割当編集・command単位と全体の既定値復元を統合設定から行える。Rustのshortcut registryを正本としてcommand集合、key正規形、1 command内の重複、command間の競合、予約済みapp/file操作、1〜4件上限を保存前とprofile import時に検証し、app-local SQLiteへatomicに保存する。既存SQLiteとstrict profile v1〜v21の単一文字列bindingは順序1の配列へ損失なく移行し、profile v22は全commandの配列を必須とする。不正・欠落・空配列・未知command・競合は保存せず局所errorとし、stale responseは現在値を置換しない。TypeScriptはkeyboard eventを正規形へ変換してRust検証済みregistryの全bindingを即時照合し、設定draftの追加・削除・競合表示とcommand dispatchだけを担当する。同一bindingを複数contextへ暗黙共有せず、system-wide hook、連続key chord、任意code実行を追加しない。Rust testで既定値、1〜4件、重複・競合・予約・上限・v21/SQLite移行・再openを、frontend testで複数bindingの同一command dispatch、追加・削除・編集・reset・save error・help表示を検証する。release WebView2の実keyboard layout、IME、AltGr、system予約key差を未観測なら未測定と記録する。 |
| REQ-LEY-P3-012 | LEY-INPUT-005 | paged Viewerで表示領域を超える画像に対し、modifierなし矢印keyは上下左右へviewportの既存10〜100%移動量でpanし、PageUp/PageDownおよび割当済み前後page commandのpage内走査にも同じ量を使う。key repeat時の加速を100〜300%で設定し、連続動作を無効にした場合は最初のkeydownだけを処理してrepeat eventを消費する。有効時はrepeatごとに加速済み量を上限内で送り、page内の該当方向が端へ達した後だけ既存の前後page/作品移動へ進む。smooth設定は既存REQ-LEY-P2-007を共有し、OSの`prefers-reduced-motion`を常に優先する。Rust settings境界は移動量、加速率、連続動作を検証してapp-local SQLiteへatomic保存し、strict profile v23で必須化する。profile v1〜v22と既存DBには移動量90%、加速150%、連続動作有効を補う。TypeScriptはfocus安全性、keyboard event repeat、現在のviewer overflowとscrollだけを扱い、設定値の正本やfilesystem操作を持たない。input・dialog・IME composition・modifier付きkeyへ干渉せず、system-wide hook、独自repeat timer、release後の慣性、原本変更を追加しない。Rust testで既定・境界・不正値・SQLite再openを、frontend testで4方向、端でのpage移動、repeat有効/無効、加速、smooth/reduced-motion、focus安全性、profile移行を検証する。release WebView2の実keyboard repeat rate、IME、layout別key差、scroll latencyを未観測なら未測定と記録する。 |
| REQ-LEY-P3-013 | LEY-INPUT-006 | catalog一覧の修飾keyなしprimary click、double click、middle button、back button、forward buttonへ、`selectOnly`、`openSelected`、戻る、進む、親folder、更新、検索pane切替、`none`の安全な既知commandを操作別に割り当てる。primary clickは割当にかかわらず選択を即時反映し、Ctrl/Meta・Shiftの複数選択、右click context menu、drag開始、favorite buttonを固定操作として優先する。primary clickに追加commandを割り当てた場合だけ250msのdouble-click判定後に1回dispatchし、double click成立時は待機中のsingle actionを取消してdouble actionだけを1回実行する。middle/back/forwardはitemを先に単一選択してbrowser既定動作を抑止し、entryを要する`openSelected`はevent時のitemを既存Rust activation境界へ渡す。Rustのcatalog mouse registryを正本として完全なgesture集合と既知actionだけをprofile保存前・import時に検証し、app-local SQLiteへatomic保存する。strict profile v24で全bindingを必須化し、profile v1〜v23と既存DBにはprimary=`selectOnly`、double=`openSelected`、middle=`none`、back=`navigateBack`、forward=`navigateForward`を補う。TypeScriptはpointer/mouse eventのbutton・modifier判定、250msの局所timer、設定draft、Rust検証済みcommandの既存App handlerへのdispatchだけを担当する。delete、move、copy、外部app、任意code、global hook、right click再割当、5 button超の機器固有入力を追加しない。Rust testで完全shape、未知gesture/action、default、SQLite再openを、frontend testでsingle/double分離、middle/back/forward、複数選択・context・drag保護、open/navigation/search/refresh、timer cleanup、profile移行・不正値拒否を検証する。release WebView2の実double-click interval、button 3/4機器差、touchpad、DPI、input latencyを未観測なら未測定と記録する。 |
| REQ-LEY-P3-014 | LEY-INPUT-008 | paged Viewerのstageを表示領域の中央でtop-left、top-right、bottom-left、bottom-rightへ4分割し、modifierなしprimary mouse clickへ象限別に`none`または既存の安全なViewer commandを割り当てる。既定は左2象限=`previousPage`、右2象限=`nextPage`とし、読書方向を反転しても物理領域の割当自体は変えない。象限は画像やpageではなくstageの`getBoundingClientRect`に対するclient座標で判定し、境界上はright/bottom側へ含める。4px以上のpointer pan・drag、48px以上の既存swipe、右button、middle/side button、wheel、touch、pen、modifier付き入力、toolbar/dialog/controlからのeventへは発火させない。single click actionは固定double click全画面切替を保護するため250ms後に1回だけdispatchし、double click成立時は待機中のsingle actionをすべて取消して全画面切替だけを1回実行する。拡大画像でも移動量4px未満のmouse clickは有効とするが、pan開始後はpointer capture終了まで象限actionを実行しない。Rustのviewer quadrant registryを正本として4象限の完全shapeと`none`を含む既知Viewer actionだけをprofile保存前・import時に検証し、app-local SQLiteへatomic保存する。strict profile v25で全bindingを必須化し、profile v1〜v24と既存DBには既定値を補う。TypeScriptはWebView pointer種別・button・modifier・座標、4px click/drag判定、250msの局所timer、設定draft、Rust検証済みactionの既存Viewer handlerへのdispatchだけを担当し、設定正本、filesystem、任意code、外部app、global hookを持たない。Rust testで完全shape、未知象限/action、default、SQLite再openを、frontend testで4象限・境界、single/double分離、pan/swipe・touch/pen・modifier保護、拡大画像click、timer cleanup、全action routing、profile移行・不正値拒否を検証する。release WebView2の実double-click interval、touchpad、pen、DPI、input latencyを未観測なら未測定と記録する。 |

| REQ-LEY-P3-015 | LEY-INPUT-009 | ページ送りViewer stageのmodifierなしright mouse clickへ、`none`または既存の安全なViewer commandを1つ割り当てる。既定は`none`として現行の無操作を保つ。mouseの右button down/upが同一pointerで4px未満の移動に収まった場合だけpointer upで1回dispatchし、右buttonを押したままのwheelは既存のright-wheel割当を優先してright-click actionを取消する。4px以上の移動、touch、pen、modifier付き入力、pointer cancel、window blur、toolbar・dialog・control、catalog・folder treeのcontext menuでは発火させない。Viewer stageのbrowser context menuは従来どおり抑止するが、catalogとfolder treeのfile operation context menuは固定操作として変更しない。Rustのviewer right-click registryを正本とし、`none`を含む既知Viewer actionだけをprofile保存前・import時に検証してapp-local SQLiteへatomic保存する。strict profile v26でbindingを必須化し、profile v1〜v25と既存DBには`none`を補う。TypeScriptはWebView pointer種別・button・modifier・座標、4px click/drag判定、right-wheel成立の取消、設定draft、Rust検証済みactionの既存Viewer handlerへのdispatchだけを担当し、設定正本、filesystem、任意code、外部app、global hookを持たない。Rust testで既知・未知action、default、profile移行、SQLite再openを、frontend testでsingle dispatch、movement・modifier・touch/pen・cancel保護、right-wheel優先、catalog context menu非回帰、設定保存を検証する。release WebView2の実right-click順序、touchpad、多ボタンmouse、DPI、input latencyを未観測なら未測定と記録する。 |

| REQ-LEY-P3-016 | LEY-INPUT-013 | ページ送りViewer toolbarの「矩形ズーム」を明示的に有効化した後、modifierなしprimary mouse dragでstage内の矩形を指定し、選択範囲の中心を保ってviewportに収まる最大倍率へ1回だけ拡大する。開始点・終了点はstage viewportへclampし、幅または高さ12 CSS px未満は無操作とする。有効な選択では現在表示倍率に`min(viewportWidth / selectionWidth, viewportHeight / selectionHeight)`を乗じ、1〜800%にclampし、選択中心のcontent座標が拡大後のviewport中央に来るscroll位置を求める。Rustの`resolve_viewer_rectangle_zoom`を計算と検証の正本とし、viewport・selection・scroll・現在倍率の有限値、正の寸法、包含、32768px viewport上限、1〜800%の倍率上下限をIPC境界で検証する。TypeScriptはWebView pointer座標・modifier・pointer capture、選択overlay、Rust応答の既存scale/scroll DOM adapterへの適用だけを担当する。armed中は通常pan、4象限click、swipe、right click、middle/side button、wheelを実行せず、適用完了後は自動で解除する。Escape、pointer cancel、window blur、Viewer/session変更、toolbar再操作では適用せず解除し、touch、pen、modifier付きdragは開始しない。selection overlayはDOM上の一時表示だけで、原本、変換state、bookmark、読書位置、profile、SQLite、cacheへ保存しない。Leeyes 2.6.1での現行可用性と起動gestureが未確認のため、ツールバーの明示toggle、1回適用、12px最小選択を安全で予測可能な現代化として採用する。Rust testで計算、clamp、不正・非有限値・上限拒否を、frontend testでoverlay、pointer capture、適用、小矩形、入力競合、cancel、session cleanup、stale/error応答を検証する。release WebView2の実pointer capture、DPI、高倍率画像、input・zoom latencyを未観測なら未測定と記録する。 |
| REQ-LEY-P3-017 | LEY-FILE-005, LEY-FILE-006, LEY-FILE-007 | 利用者がWindows native file pickerで明示選択した実在する`.exe`だけを外部アプリとして最大16件登録し、表示名、固定引数、対象mode（先頭項目・選択項目すべて・先頭項目の親folder）をapp-local SQLiteへ保存する。Rustをregistry、入力検証、canonical executable identity、library containment、launch preview、process起動、履歴の正本とし、登録時と起動時に実行fileを再検証する。固定引数は最大16件・各256文字、対象は最大64件とし、NUL・改行・control文字を拒否する。起動はshellやcommand-line文字列を使わず`Command::new`と個別の`arg`だけで構成し、placeholder展開、DLL/plugin load、環境変数展開、作業directory変更を行わない。起動前にアプリ名、target mode、対象件数、固定引数件数をpreviewし、利用者が確認した同一app・同一選択に限りlaunchする。登録・更新・削除は専用commandへ分離し、更新ではpicker由来の実行fileを差し替えない。成功した起動履歴は新しい順に20件へ制限し、app ID、表示名、mode、対象件数、時刻だけを保存してfile path・引数を履歴へ残さない。frontendは設定draft、native picker開始、catalog選択の収集、確認dialog、Rust応答表示とTauri orchestrationだけを担当し、path検証、quoting、allowlist、履歴正本、process起動を持たない。既存のWindows「アプリケーションから開く…」は固定OS操作として維持する。Rust testでschema移行、上限、入力拒否、再open、preview/launch plan、path containment、個別引数、history privacy・上限を、frontend testで登録管理、複数選択preview、確認/cancel、stale/error、既存OS chooser非回帰を検証する。release製品でのnative picker、第三者app起動、UAC、長いUnicode path、removable executable、履歴表示、起動時間を未観測なら未測定と記録する。 |
| REQ-LEY-P3-018 | LEY-FILE-022 | 1件の名前変更では既定で拡張子を除くbasenameだけを入力選択し、保存済み設定で拡張子を含む全体選択へ切り替えられる。2〜256件の選択では基底名、区切り文字（空・space・`-`・`_`）、開始番号0〜999999、桁数1〜6、元拡張子保持を指定し、catalog順の各項目へ`基底名 + 区切り + zero-pad連番 + 拡張子`を割り当てる。Rustを設定検証・SQLite永続化、source containment、拡張子分解、Windows名検証、重複・既存衝突検出、preview key、実renameの正本とする。previewは変更前後の相対pathを最大256件返しfilesystemを変更せず、利用者が確認した同一source・同一設定のopaque keyだけを実行で受理する。実行はfile-operation mutex内でplanを再計算し、途中失敗時は完了済み項目を逆順rollbackして、rollback失敗を隠さない。source自身と同名は無操作として除外し、別sourceの現行名、既存項目、case-insensitive生成名重複、root・parentをまたぐ変更、reparse、欠落を拒否する。frontendはWebView input selection、設定draft、選択順の収集、preview/確認dialog、Tauri orchestrationだけを担当し、名前生成、path検証、衝突判定、renameを持たない。Rust testで設定境界・再open、拡張子、連番、Unicode、衝突、無操作、preview一致、rollbackを、frontend testで1件の選択範囲、複数選択preview、確認/cancel、stale/errorを検証する。release filesystemの256件rename時間、case-only名、長いUnicode名、removable/network drive、watcher反映、rollback障害を未観測なら未測定と記録する。 |
| REQ-LEY-P3-019 | LEY-SETTING-004 | 用途別の設定snapshotを1〜64 UTF-16 code unitの名前でapp-local SQLiteへ最大16件保存し、一覧から現在使用するprofileを再起動なしで切り替えられる。名前はtrim後に空、control文字、slashを拒否し、大小文字を無視して一意とする。新規保存と明示的な上書きは現在のstrict `SettingsProfileInput`全体をRustで再検証し、library root、履歴、bookmark、file pathその他端末固有情報を含めない。切替previewはprofile名、現在値から変わるfield数、strict profile、snapshot内容に結び付くopaque keyを返すが設定を変更せず、利用者が同じsnapshotを確認した場合だけ実行する。実行直前にRustがsnapshotの存在、JSON schema、全field値、opaque keyを再検証し、既存settings全体とactive profile名を1 SQLite transactionで保存する。active profileは一覧へ表示し、内容と現行settingsの一致を保つためactive中の削除と上書きを拒否する。通常の設定適用または個別設定変更はsnapshotを暗黙上書きせずactive表示を解除し、既存snapshotを変更する場合は明示上書きを要求する。破損・欠落・stale key・保存失敗では現行settingsとactive表示を保持する。always-on-top等のnative window side effectはfrontendが実行前に適用し、Rust保存失敗時は元へ戻す。tray preferenceは成功後に更新し、hidden visibilityとfolder watchは既存の設定適用後処理を再利用する。Leeyesの任意設定fileを直接標準保存先へする方式は権限、更新、破損、path漏えいを避けるため導入せず、同等の用途別即時切替をapp-local named snapshotとして独自実装する。TypeScriptは名前入力、一覧、preview/確認、native window adapter、成功したRust profileのReact state反映、Tauri orchestrationだけを担当し、schema検証、snapshot正本、active判定、永続化を持たない。Rust testでschema v8移行、名前・件数・case一意性、上書き、active削除・上書き拒否、破損、preview key、atomic切替・通常保存時の解除、再openを、frontend testで一覧・保存・上書き確認・切替確認・state反映・cancel・stale/error・native rollbackを検証する。release WebView2の切替表示、16 profileの応答時間、DB障害復旧、長いUnicode名を未観測なら未測定と記録する。 |
| REQ-LEY-P3-020 | LEY-IO-001, LEY-IO-002, LEY-IO-003, LEY-IO-004, LEY-IO-005, LEY-IO-006 | CSV出力設定を1〜64 UTF-16 code unitの名前でapp-local SQLiteへ最大32件保存し、大小文字を無視して一意なpresetとして一覧・明示上書き・確認付き削除できる。Rustを唯一のCSV schema・preset・生成正本とし、1〜12個の重複しないordered columnを `name`、`stem`、`extension`、`kind`、`relativePath`、`size`、`modifiedMs`、`namePart1`〜`namePart4` から選択でき、header有無、size単位 `bytes` / `kib` / `mib`、任意のliteral filename delimiterを保存・再検証する。delimiterはtrimせず文字列どおりにstemを最大4部分へ分割し、空、control文字、slash、8 UTF-16 code unit超過を拒否する。Leeyesで不明な丸めは安全で決定的な独自仕様としてbytesを整数、KiB/MiBを小数2桁とする。対象は選択項目、現在folder直下、現在folder以下recursiveの3 scopeとし、Rustが設定済みlibrary root内を再列挙する。選択scopeは現在folderの直接子との完全一致だけを許可し、recursiveはsymlink/reparse-pointを追跡せず、深さ64、50,000行、16 MiBでfail closedする。出力はUTF-8 BOM、CRLF、RFC 4180互換quoteとし、`= + - @ TAB CR`から始まる全cellをapostropheで無害化し、absolute pathを含めない。presetはscopeを保存せず、外部任意CSV設定fileや対象外CLI機能を導入しない。TypeScriptはdialog入力、preset操作確認、scope/選択値、Tauri orchestration、Rustが返したbyte列のdownloadだけを担当し、CSV escape、列schema、単位計算、filesystem traversalを持たない。Rust testでschema v9移行、名前・件数・case一意性、strict JSON、列順/header/単位/分割、formula無害化、scope containment、symlink、深さ・行・容量上限、50,000行生成性能を、frontend testでdialog、preset、確認、scope、download/errorを検証する。release WebView2の保存dialog、実library 50,000項目、長いUnicode delimiter、DB障害復旧を未観測なら未測定と記録する。 |
| REQ-LEY-P3-021 | LEY-IO-007, LEY-IO-008, LEY-IO-009 | Windows製品実行fileの起動引数と後続起動引数から、Rustが最大1個のfileまたはfolder pathと、排他的なlaunch mode `normal` / `fullscreen` (`-f`, `--fullscreen`) / `slideshow` (`-s`, `--slideshow`) を解析する。OSが分割したargumentをそのまま受け取り、shell文字列の再解析、quoteの独自分割、environment変数展開、wildcard展開を行わない。`--`以降はoptionで始まるpathとして扱い、相対pathは対応する起動のcurrent working directoryだけを基準に解決する。未知option、mode重複・競合、path複数、NUL/control、32,767 UTF-16 code unit超過を拒否する。Rustはfilesystem上の実在、canonical path、file/folder、対応形式、readabilityを検証し、folderはそのfolderをlibrary rootとしてcatalogを開き、fileは親folderをlibrary rootとして対象のviewerを開くRust launch planに正規化する。folderへの`-f`/`-s`は対象folder自体をcomic viewerで開く。Tauri公式single-instance pluginをRust側の最初のpluginとし、後続instanceを新たに残さず、そのargumentsとcwdを既存instanceの有界FIFO（最大16件）へ渡し、main windowをshow・unminimize・focusする。queue溢れ、不正path、unsupported、access deniedは任意codeを実行せず局所通知し、現在のviewer/libraryを壊さない。TypeScriptはRustが返す検証済みlaunch planをFIFO順に取得し、既存のlibrary登録・catalog読込・viewer開始・全画面・slideshowのReact/Tauri orchestrationだけを担当する。argument parser、path結合・cwd解決、canonicalization、file kind判定、single-instance IPC、queue正本をTypeScriptに持たない。Rust testでspace・Unicode・relative・`--`、alias、unknown・重複・競合・複数path・control・長さ、file/folderのplan、unsupported/missing/access、FIFO上限を、frontend testでstartup・後続event、folder/file、normal/fullscreen/slideshow、順序、error・staleを検証する。release製品の実single-instance引渡し、Windows Terminal/PowerShell/cmd/Explorer別quoting、UNC・長path、network/removable drive、起動・focus時間、CPU・working setを未観測なら未測定と記録する。 |
| REQ-LEY-P4-001 | LEY-SHELF-001, LEY-SHELF-003, LEY-SHELF-004, LEY-SHELF-005, LEY-SHELF-007, LEY-SHELF-008, LEY-SHELF-009 | 複数の名前付き仮想本棚と、その配下の任意深さの仮想folder・参照itemをapp-local SQLiteで管理する。Rustを本棚名、node階層、対象absolute path、対象kind、兄弟順、組込みicon preset、起動時本棚、import/export形式の唯一の正本とし、TypeScriptはdialog、tree表示、drag payloadの選択、確認、Rustが検証したopen planの既存library/viewerへの適用だけを担当する。本棚名・node名はtrim後1〜64 UTF-16 code unit、control・slashなし、同一親内でcase-insensitive一意とし、本棚は最大64、各本棚nodeは最大10,000、階層は64までとする。catalogまたはfolder treeからの登録は現在のRust library root内にある最大256項目を対象とし、canonicalize、containment、実在、reparse、対応kindをRustが検証して同一参照を重複登録しない。drag and dropはopaqueな内部payloadと明示drop targetだけを受理し、外部native path、任意text、HTML、URLを本棚登録へ解釈しない。仮想folderの作成・rename・移動・除去とitemの移動・除去は実filesystemを変更せず、folder除去は子孫件数をpreviewして確認後にDBだけをtransactionで更新する。兄弟順はRustの整数順序をatomicに再採番し、iconは`books`・`folder`・`star`・`archive`・`image`の同梱presetだけを保存して任意画像・SVG・codeを読み込まない。テキスト入出力はUTF-8 BOM付きJSON Linesのversion 1とし、先頭metadata行、shelf行、親より後に並ぶnode行をRustが生成する。importは16 MiB、64本棚、合計50,000 node、行長64 KiB、深さ64を全件parse・schema検証したread-only previewとopaque keyへ固定し、同名衝突を新規作成または明示置換として表示し、同じbytesとmodeを確認した場合だけ1 transactionで反映する。任意code、外部resource、filesystem書込み、参照先の自動作成・修復を行わない。消失参照の整理はRustが明示実行時に最大10,000参照をread-only検査してmissing候補を返すが、access denied、offline、removable/networkの一時不在をmissingとして自動削除せず、利用者が候補を確認した場合だけ選択IDをDBから除去する。起動時本棚は存在する本棚1件または無効を保存し、起動時に本棚dialogを開いて選択本棚を展開するだけで、参照先を自動openせず、削除された指定は無効へ回復する。旧root別localStorage本棚はfrontendが読み取った相対pathを最大1,000件の明示migration payloadとして一度だけRustへ渡せるが、Rustが対応rootと対象を再検証して既定の「移行済み」本棚へ登録成功した後だけ旧dataを消去する。参照itemを開くとRustが現在の実在・canonical path・kindを再検証し、folderはそのfolderをlibrary rootとしてcatalog、fileは親folderをrootとしてviewerを開くplanを返す。欠落・access・stale・DB障害は現在本棚とlibraryを保持する。Leeyesの不明なtext形式、並べ替えUI、任意icon画像はコピーせず、versioned JSON Lines、上下移動、組込みpresetを安全で移行可能な独自仕様として採用する。Rust testでschema v10移行、全CRUD、一意性・上限・cycle・順序、登録containment、実体非破壊、cleanup分類、startup回復、JSONL round-trip・破損・上限・preview key・transaction rollback、legacy migration、open再検証を、frontend testでtree、create/rename/remove確認、DnD内部payload、import/export、cleanup、startup選択、並べ替え・icon、open plan適用、stale/errorを検証する。10,000 nodeの一覧・並べ替え・missing検査と50,000 node import previewを測定し、release WebView2の大規模tree FPS・working set、network/removable/offline判定、native drag、保存dialog、起動時間を未観測なら未測定と記録する。 |
| REQ-LEY-P4-002 | LEY-FILER-002, LEY-FILER-003, LEY-FILER-004 | 現在libraryの対応書庫（ZIP/CBZ/EPUB、RAR/CBR、7z/CB7、LZH/LHA）をfilesystem folder treeへread-onlyの展開可能nodeとして表示し、Rustが書庫absolute pathを現在root内へcanonicalize・containment・regular-file・非reparse・対応kindとして再検証した場合だけ仮想tree snapshotを返す。snapshotはroot書庫、書庫内folder、画像、入れ子書庫をopaque node ID、opaque parent ID、表示名、kind、hasChildren、画像だけの既存opaque page key、兄弟sort orderとして返し、TypeScriptはpathや書庫形式をparseせずtree/listの表示・選択と既存Viewerへのpage選択適用だけを担当する。書庫内容の一覧は通常folderと同じ保存済み表示形式（詳細リスト、小サムネイル、表紙グリッド、カードグリッド、情報カード）、thumbnail寸法、palette、詳細書式を共有し、toolbarでの表示形式変更を即時反映する。書庫内の選択・hover・focus状態は各paletteで背景と文字を判読可能な組合せに保つ。書庫内folderは対応画像または入れ子書庫のsafe relative entry pathから推論し、dot-prefixed entry、空folder、unsupported file、PDF本文、危険pathを表示・抽出しない。treeまたは一覧のfolder・入れ子書庫・画像は1clickで選択し、double clickまたはEnterでfolderのdirect child表示または画像Viewerを開く。選択中の画像は明示buttonまたはCtrl+Cから、Rustが既存のroot containment・opaque page key・画像decode／clipboard payload上限を再検証した場合だけWindows画像clipboardへコピーできるが、仮想fileの抽出やCF_HDROPは行わない。画像を開くとroot書庫を既存read-only Viewerで開いてRustが発行したpage keyと完全一致するpageから開始する。仮想locationはsession限定でnavigation history、address filesystem path、SQLite、profileへ保存せず、library/folder移動・root変更・missing・stale・error時は通常catalogへ安全に戻せる。仮想nodeへのrename、delete、paste、drop、filesystem copy/move、path copy、外部app起動、任意code/plugin load、原本書換え、書庫全体の展開を提供しない。入れ子書庫は既存readerと既存の深さ3、最大64書庫、累積一時byte 512 MiB、各entry 256 MiB、各書庫100,000 entry・1 GiB展開量に従い、仮想node合計50,000件でも停止する。一時書庫はapp-local tempへ衝突しない名前で作り、各走査・読取境界の終了時に削除する。Leeyesの正確な入れ子深さ・空folder・unsupported項目表示は不明なため、既存の検証済み多重圧縮上限と対応画像中心のread-only表示を安全な独自仕様として採用する。Rust testで全対応adapterの階層、natural order、重複名を持つ別parent、危険path、入れ子、深さ・個数・byte・node上限、opaque page key、temp cleanup、原本差分0、画像clipboardを、frontend testで書庫node展開、tree/list同期、全表示形式と配色、single click選択、double click／Enter open、選択画像clipboard、通常folder復帰、stale/error、filesystem operation非表示を検証する。20,000 direct entryと50,000 synthetic virtual nodeの構築時間を測定し、release WebView2の50,000 node FPS・working set、RAR/7z/LZH実大規模書庫、slow/removable drive、一時領域peak、Viewer遷移時間を未観測なら未測定と記録する。 |
| REQ-LEY-P4-003 | LEY-FILE-016 | file-operation portで最後に成功したapp所有の可逆変更をsession内1段だけRust operation journalへ保持し、編集menuとcatalog focus中のCtrl+Zから「元に戻す」を実行できる。対象はlibrary root内で完結する単一・一括rename、folder作成、copy、move、cut後paste、copy後paste、内部drag transfer、および外部native dropによってlibrary内へ作成したcopyとする。Rustは各変更のoriginal/current absolute path、操作kind、件数、file種別・size・mtimeとdirectory配下のrelative name・種別・size・mtimeからなる最大50,000 nodeのbounded fingerprintを変更直後に記録し、statusと実行時に現在rootのcanonical identity、全current pathの存在・非reparse・fingerprint一致、全復元先の非存在をmutex内で再検証する。copy・folder作成のundoは変更後から変化していない作成物だけを削除し、rename・moveのundoは変更後pathから元pathへ戻す。複数move/renameの途中失敗は完了分を逆順rollbackし、複数作成物の削除途中失敗は残存journalを保持して部分完了を隠さない。成功後はjournalを消費し、redoや再起動後の復元は提供しない。ごみ箱移動、完全削除、library外folder pickerへのcopy/move、Explorerへのdrag、外部app、clipboard設定など安全にapp単独で逆操作できない処理はjournal対象外とし、成功した不可逆filesystem変更は旧journalを無効化する。fingerprint取得が上限・access・時刻範囲で安全に完了しない変更も成功自体は維持するがundo不可とし、旧journalを無効化する。status/error/cancel/staleではfilesystemを変更せず、外部変更、衝突、root切替を利用者へ局所通知して現在一覧を再列挙する。TypeScriptはRustが返すavailable・operation・affectedの表示、Ctrl+Zのediting/Viewer/tree除外、typed IPC、成功後の既存catalog/tree refreshだけを担当し、path、fingerprint、journal、削除・rename・copy/move logicを持たない。LeeyesのOS shell履歴をそのまま呼ぶ方式は採らず、Comic Explorerが観測・検証できる直近操作だけを安全に戻す独自仕様とする。Rust testで各対象操作、1段置換、成功消費、不可逆操作無効化、root不一致、外部変更・復元先衝突、reparse、50,000 node上限、複数rename/move rollback、copy削除の部分失敗を、frontend testでmenu enable/label、Ctrl+Z、editing/Viewer/tree保護、成功refresh、error/cancel/stale保持を検証する。10,000作成物のjournal fingerprintとstatus検証を2秒未満で測定し、release WebView2の実menu/shortcut、network/removable drive、外部processとの競合、巨大directory、CPU・working setを未観測なら未測定と記録する。 |
| REQ-LEY-P4-004 | LEY-SHELL-007 | 通常catalog画面の一覧ペインをnavigation/searchペインに対して右（既定）・左・上・下へ配置でき、選択を再起動、named profile保存・切替、profile export/import後も復元する。Rustを配置値 `right` / `left` / `top` / `bottom`、縦配置時のnavigation幅180〜480px、横配置時のnavigation高さ120〜480px、app-local SQLite永続化、strict profile v27検証・旧profile既定補完の正本とし、不正なDB値は右・幅240px・高さ240pxへ局所回復する。TypeScriptはRustが返した値を設定dialogへ表示し、CSS Gridの明示area、separatorのorientation・ARIA値、pointer/keyboard resizeへ適用するだけとし、独自永続化や未検証値を持たない。左・右ではnavigation/search、separator、catalogを横方向へ、上・下では同じ3領域を縦方向へ並べ、一覧の位置名どおりcatalogを配置する。pane非表示時はseparatorを除去してcatalogを全領域へ広げ、検索ペインはfolder treeと同じnavigation area・保存済み寸法を使う。配置切替、resize、root移動、検索、Viewer往復では現在folder、選択、catalog scroll、tree展開、検索条件を意図せず初期化せず、狭いwindowでは各paneを`minmax(0, ...)`で縮退させてdocument全体のoverflowを増やさない。folder別scrollはsession中に保持し、back/forward/upで戻る対象folderの正確な位置復元を選択項目の自動scrollより優先する。separatorは縦配置で左右矢印、横配置で上下矢印を10px単位で処理し、pointer座標はworkspace境界を基準に同じ上限へclampする。Leeyesの任意dock、浮動window、pixel単位の配置互換は導入せず、安全な4方向固定layoutとして独自実装する。Rust testで既定値、DB round-trip、不正値回復、profile v1〜v26移行、strict v27・named profile切替を、frontend testで4配置のarea順、separator orientation/keyboard/pointer、tree非表示、検索ペイン、設定適用・再読込、Viewer往復非回帰を検証する。4配置それぞれ10,000 catalog itemのlayout helper処理を測定し、release WebView2の10,000 item FPS・reflow時間、DPI別pointer、最小window、CPU・working setを未観測なら未測定と記録する。 |

REQ-LEY-P4-002 の書庫選択時は別dialogを開かず、通常catalogと同じ右側一覧ペインを書庫内容へ切り替える。headerには書庫名と現在階層、親階層への移動、通常folder一覧へ戻る操作を表示し、folder・入れ子書庫はdirect child一覧を切り替え、画像は既存Viewerへ開く。filesystemのaddressとhistoryは変更しない。

## Leeyes P5-A オフラインメディア台帳の受入条件

| Requirement | Leeyes ID | 受入条件 |
|---|---|---|
| REQ-LEY-P5-001 | LEY-MEDIA-001, LEY-MEDIA-002, LEY-MEDIA-003, LEY-MEDIA-004, LEY-MEDIA-005, LEY-MEDIA-008, LEY-MEDIA-009 | 現在のlibrary rootが属するWindows filesystem volumeを、Rustがvolume serial・filesystem名・rootからの相対subpathで識別し、表示名、組込みicon、相対entry path、種別、size、更新時刻、最大256件のcover thumbnailをapp-local SQLiteへ保存する。sourceへは書き込まず、UNC/network、reparse point、深さ64超、50,000 entry超、64媒体超、1 thumbnail 1MiB超、媒体あたりthumbnail合計64MiB超を拒否または上限で停止する。登録はRustのblocking scanで全snapshotを組み立ててから単一transactionで新規identityを確定し、cancelまたは失敗時は媒体・entry・thumbnailの部分行を一切残さない。同一identityの暗黙更新・差分比較はLEY-MEDIA-006/007の対象外境界を守って拒否する。媒体不在でも保存済みtreeとthumbnailを閲覧でき、availabilityは要求時にRustが接続volume identityを再照合して返す。開く操作は接続中の同一identityと保存subpathを再検証できた場合だけ既存の安全なlibrary navigationへ渡し、不在時は台帳表示を維持して局所errorにする。独自iconは任意画像読込みでなく検証済み組込みpresetから選びRustへ永続化する。TypeScriptはtyped IPC、dialog表示、cancel/refresh/open要求だけを担い、identity、scan、検証、transaction、永続化の正本を持たない。Rust testでidentity、relative containment、scan上限、reparse除外、atomic cancel/失敗、duplicate拒否、offline snapshot、availability、icon enum、thumbnail byte上限、SQLite migration/round-tripを、frontend testで登録・cancel・不在表示・tree・thumbnail・icon・open/error保持を検証する。50,000 entry snapshotのscan/DB処理とthumbnail上限を測定し、release実機の光学media、drive letter変更、50,000実file走査、巨大画像decode、CPU・working setを未観測なら未測定と記録する。 |

## Leeyes P5-B 非破壊画像フィルターの受入条件

| Requirement | Leeyes ID | 受入条件 |
|---|---|---|
| REQ-LEY-P5-002 | LEY-FILTER-001, LEY-FILTER-002, LEY-FILTER-003, LEY-FILTER-004, LEY-FILTER-005, LEY-FILTER-006, LEY-FILTER-007, LEY-FILTER-008, LEY-FILTER-009, LEY-FILTER-010, LEY-FILTER-011, LEY-FILTER-012, LEY-FILTER-013, LEY-FILTER-014, LEY-FILTER-015, LEY-FILTER-016 | Rustを画像処理、filter parameter検証、ordered chain、named set、active set、app-local SQLite永続化の正本とし、Viewerへ渡すpage byteだけを変換して原本・thumbnail cache・書庫・PDFを変更しない。named setは最大32件、名前1〜64文字、chainは最大16 stepとし、stepごとの有効/無効と順序を保存・再利用できる。提供stepはluminance grayscale、black/white/gamma levels、gamma 0.10〜5.00、contrast・brightness -100〜100、RGB channel histogram equalization、posterize 2〜32階調、RGB invert（alpha維持）、2〜16点のpiecewise-linear tone curve、sharpen amount 0.10〜3.00、unsharp radius 1〜8・amount 0.10〜3.00・threshold 0〜255、Gaussian blur radius 1〜8、各辺0〜45%かつ対向辺合計90%未満の非破壊crop、各辺0〜512px・`#RRGGBB`色のmarginとする。Leeyesで未確認の既定値・値域・色空間は安全で再現可能な独自仕様としてUIへ明示する。フィルターdialogはセット選択、選択中セットの保存・有効化・削除、順序付き手順編集を意味単位で分け、現在有効なセットとstep数を明示する。狭いdialog幅では1列へ切り替え、横scrollbarや操作buttonの切り落としを発生させず、少ない手順では不要な固定空白を作らない。RustはWICで既存対応画像、書庫entry、PDF renderをRGBAへdecodeし、chain順にalphaを保持して処理し、bounded PNGとして既存media grantへ戻す。最大辺16,384px、120,000,000pixel、256MiB source/output、最大512,000,000 pixel-stepを越える処理、crop後0 dimension、margin後の既存画像上限超過を実行前に拒否する。active set変更時はViewerの現在anchorを保持してmedia grantを再取得し、stale generationを表示しない。TypeScriptはtyped draft、追加・削除・並べ替え、組込みparameter入力、保存・active切替と再読込要求だけを担当し、pixel演算、chain validation、SQL、独自永続化を持たない。Rust testで全14処理の代表pixel、chain順序差、alpha、crop/margin寸法、parameter・resource上限、SQLite migration/CRUD/active切替を、frontend testでnamed set、16-step上限、順序、有効状態、全step入力、current-page保持、error/stale保持を検証する。4K RGBA画像へ代表chainを適用した時間と出力上限を測定し、release WebView2の実画像品質・色管理・animated GIF frame・CPU/GPU・peak working setを未観測なら未測定と記録する。 |

## 非採用と将来候補の境界

| 区分 | 安定ID | 扱い |
|---|---|---|
| Candidate | FUT-C-052 | file operation undo。OS shellのundo履歴をapplication状態として保証できるまで未採用。 |
| Candidate | FUT-R-006, FUT-R-007 | touch、gamepad。実機契約と直接観測ができるまで未採用。 |
| Rejected | FUT-R-001, FUT-R-002, FUT-R-003, FUT-R-008 | cloud同期、外部書誌、外部送信、閲覧時の原本自動変更。恒久安全原則を変更しない限り採用しない。 |
| Partial | FUT-C-006, FUT-C-007, FUT-C-008 | AVIFはunsupported/parser境界だけ実装済み。完全decodeを推定しない。 |

FR-B04は現行の採用laneとして定義されていない。欠番を新機能の根拠として扱わない。

## Leeyes viewer操作・外観の受入条件

| 要件ID | Leeyes ID | 受入条件 |
|---|---|---|
| REQ-FR-B23-001 | LEY-VIEWER-004 | page layoutかつ見開き表示のtoolbarに「1ページ戻す」「1ページ進める」を説明付きicon buttonとして置く。操作は現在の見開きanchorを自然順で正確に1pageだけ移動し、読み方向に従う左右配置、横長pageの単独表示、読書位置保存を維持する。先頭・末尾では範囲外へ進まず、巻頭・巻末の作品移動を発火しない。単pageとscroll layoutでは無効にする。 |
| REQ-FR-B23-002 | LEY-VIEWER-025 | viewer設定で背景を市松模様・濃灰・黒・明色から選び、page周囲余白と見開き間隔をそれぞれ0〜64pxの整数で指定する。既定値は市松模様・0px・8pxとする。設定値はpage layoutの単page・見開きおよびscroll layoutへ適用し、見開き画像の利用可能幅計算にも同じ間隔を用いる。 |
| REQ-FR-B23-003 | LEY-VIEWER-028 | viewer設定でcursor自動非表示を無効・1秒・2秒・3秒・5秒から選ぶ。有効時はpointerが画像stage内で指定時間操作されなければstage上のcursorだけを隠し、pointer移動・再入場・button操作で直ちに再表示する。pointer drag中およびルーペ有効時はcursorを隠さず、toolbar・page移動bar上のcursorへ影響させない。既定値は無効とする。 |
| REQ-FR-B23-004 | LEY-VIEWER-025, LEY-VIEWER-028 | 背景、余白、間隔、自動非表示時間はapp-local SQLiteと設定profile v5へ保存し、再起動とexport/importで復元する。profile v1〜v4と既存SQLiteに値がない場合は安全な既定値へ移行する。frontendとbackendの両方でenum・整数範囲を検証し、不正値を適用・保存しない。 |

## アプリテーマの受入条件

| 要件ID | 受入条件 |
|---|---|
| REQ-FR-B24-001 | 統合設定の画面カテゴリで、Windowsのlight/dark変更へ実行中も追従するsystemと、stable ID `light`、`dark`、`paper`、`midnight`、`oled`、`forest`、`highContrast`の7組込みテーマ、および保存済みカスタムテーマから1件を選ぶ。新規利用と全設定resetの既定はsystemとし、固定テーマとカスタムテーマはOSのlight/dark変更で配色を変えない。組込みテーマは直接編集せず、複製してカスタムテーマにできる。設定選択は既存draftに留め、明示Apply成功後だけ現在のアプリへ反映する。 |
| REQ-FR-B24-002 | 選択テーマはdocument rootのsemantic CSS custom propertyとしてshell、menu、toolbar、address/status bar、tree/search、catalog chrome、context menu、empty/error/status面、全dialog、settings/help/about/license、viewer toolbar/page navigator、form control、scrollbar、および通常・hover・focus・selected・disabled・danger状態へ適用する。fullscreenで画像上に重なるviewer controlは可読性のため固定半透明dark surfaceを許容する。catalogは常にアプリテーマを継承する。viewer stageの`viewerBackground`、原画像、thumbnail、filter出力、gridは変更しない。 |
| REQ-FR-B24-003 | カスタムテーマschema v1は名前1〜64 UTF-16 code unit、`baseScheme` light/dark、および`#RRGGBB`のcanvas、surface、surfaceMuted、surfaceRaised、text、textMuted、border、accent、onAccent、selection、onSelection、focus、danger、onDanger、warning、successの完全なsemantic color集合だけを持つ。統合設定内のeditorで新規作成、組込み・カスタムからの複製、名前変更、color input編集、代表的なshell・control・selection・状態の局所previewを行う。任意CSS、HTML、SVG、画像、font、path、URL、外部resource、script、layout、spacing、animationをthemeとして受理しない。 |
| REQ-FR-B24-004 | Rustをcustom theme ID、Unicode full case-foldによるname一意性、最大32件、完全shape、unknown field拒否、1件64 KiB以下、色形式、contrast、CRUD、import preview、明示replace、export byteの正本とし、app-local SQLite schema v13へatomicに保存する。JSON import/exportはUTF-8、`schemaVersion: 1`、単一themeだけを扱い、importは保存前previewとopaque confirmation keyを必須とする。同名themeを暗黙上書きしない。現在適用中のcustom themeは別themeのApply成功前に更新・同名import置換・削除できず、複製して編集する。破損recordも32件上限へ算入して名前と理由を表示し、自動削除せず、実効themeが別themeである場合だけ二段階確認から明示削除できる。TypeScriptはvalidated DTOのeditor、preview、download/upload、Tauri orchestrationだけを担当する。 |
| REQ-FR-B24-005 | theme selectionとcustom snapshot/revisionをapp-local settings、strict frontend profile v29、named profile、profile export/importへ含める。custom themeを使うexport/named snapshotには正規化済みdefinitionを埋め込み、別環境でthemeが欠落してもimport previewと明示Applyで安全なcopyとして復元できるようにする。既存DBとprofile v1〜v27、旧named snapshotには従来外観を維持するlightを補い、v28のテーマ、pane配置、Viewer象限、右click設定を失わない。欠落・破損・削除済み参照はlightへ局所fallbackして通知し、破損recordを自動削除しない。 |
| REQ-FR-B24-006 | 通常文字、補助文字、accent上文字、selection文字は使用surfaceに対して4.5:1以上、focusと主要境界は隣接surfaceに対して3:1以上をfrontend previewとRust保存境界の両方で検証する。Windows `forced-colors: active`ではOS色をthemeより優先し、app独自highContrastをOS high contrast対応の根拠にしない。resolved `color-scheme`をform/scrollbarへ渡し、native title barはsystemでnative追従、組込み・customでは`baseScheme`へ合わせる。native theme適用失敗時はsettingsを保存せず、backend保存失敗時はnative themeを旧値へrollbackし、DOMは成功したselectionだけを描画する。初回theme未取得時はsystemへ安全にfallbackし、既存の100ms起動待機上限を延ばさない。 |

## PDF対応の受入条件

| Feature | 要件ID | 現行契約 |
|---|---|---|
| FR-B21 PDF viewer | REQ-MVP-020, REQ-FR-B21-001, REQ-FR-B21-002, REQ-FR-B21-003 | `.pdf`をcatalogの`pdf`種別で表示し、standalone documentのpage数を列挙する。root包含確認で得たWindows canonical pathをWindows.Data.Pdfが受理できる通常pathへ変換し、各pageは寸法を検証してからboundedな出力寸法を指定してPNG renderし、既存viewer、thumbnail、favorite、巻末遷移、読書位置を利用する。PDFは書庫内entryや書庫として再帰解釈せず、暗号化・破損・0 page・過大source/render・root外symlinkを分類して拒否する。 |
